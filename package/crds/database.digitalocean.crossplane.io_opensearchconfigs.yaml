---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: opensearchconfigs.database.digitalocean.crossplane.io
spec:
  group: database.digitalocean.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - do
    kind: OpenSearchConfig
    listKind: OpenSearchConfigList
    plural: opensearchconfigs
    singular: opensearchconfig
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OpenSearchConfig is the Schema for the OpenSearchConfigs API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OpenSearchConfigSpec defines the desired state of OpenSearchConfig
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  actionAutoCreateIndexEnabled:
                    description: 'Specifices whether to allow automatic creation of
                      indices. Default: true'
                    type: boolean
                  actionDestructiveRequiresName:
                    description: Specifies whether to require explicit index names
                      when deleting indices.
                    type: boolean
                  clusterId:
                    description: The ID of the target Opensearch cluster.
                    type: string
                  clusterIdRef:
                    description: Reference to a Cluster in database to populate clusterId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  clusterIdSelector:
                    description: Selector for a Cluster in database to populate clusterId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  clusterMaxShardsPerNode:
                    description: Maximum number of shards allowed per data node.
                    type: number
                  clusterRoutingAllocationNodeConcurrentRecoveries:
                    description: 'Maximum concurrent incoming/outgoing shard recoveries
                      (normally replicas) are allowed to happen per node. Default:
                      2'
                    type: number
                  enableSecurityAudit:
                    description: 'Specifies whether to allow security audit logging.
                      Default: false'
                    type: boolean
                  httpMaxContentLengthBytes:
                    description: 'Maximum content length for HTTP requests to the
                      OpenSearch HTTP API, in bytes. Default: 100000000'
                    type: number
                  httpMaxHeaderSizeBytes:
                    description: 'Maximum size of allowed headers, in bytes. Default:
                      8192'
                    type: number
                  httpMaxInitialLineLengthBytes:
                    description: 'Maximum length of an HTTP URL, in bytes. Default:
                      4096'
                    type: number
                  indicesFielddataCacheSizePercentage:
                    description: Maximum amount of heap memory used for field data
                      cache, expressed as a percentage. Decreasing the value too much
                      will increase overhead of loading field data. Increasing the
                      value too much will decrease amount of heap available for other
                      operations.
                    type: number
                  indicesMemoryIndexBufferSizePercentage:
                    description: 'Total amount of heap used for indexing buffer before
                      writing segments to disk, expressed as a percentage. Too low
                      value will slow down indexing; too high value will increase
                      indexing performance but causes performance issues for query
                      performance. Default: 10'
                    type: number
                  indicesMemoryMaxIndexBufferSizeMb:
                    description: Maximum amount of heap used for indexing buffer before
                      writing segments to disk, in mb. Works in conjunction with indices_memory_index_buffer_size_percentage,
                      each being enforced. The default is unbounded.
                    type: number
                  indicesMemoryMinIndexBufferSizeMb:
                    description: 'Minimum amount of heap used for indexing buffer
                      before writing segments to disk, in mb. Works in conjunction
                      with indices_memory_index_buffer_size_percentage, each being
                      enforced. Default: 48'
                    type: number
                  indicesQueriesCacheSizePercentage:
                    description: 'Maximum amount of heap used for query cache. Too
                      low value will decrease query performance and increase performance
                      for other operations; too high value will cause issues with
                      other functionality. Default: 10'
                    type: number
                  indicesQueryBoolMaxClauseCount:
                    description: 'Maximum number of clauses Lucene BooleanQuery can
                      have. Only increase it if necessary, as it may cause performance
                      issues. Default: 1024'
                    type: number
                  indicesRecoveryMaxConcurrentFileChunks:
                    description: 'Maximum number of file chunks sent in parallel for
                      each recovery. Default: 2'
                    type: number
                  indicesRecoveryMaxMbPerSec:
                    description: 'Limits total inbound and outbound recovery traffic
                      for each node, expressed in mb per second. Applies to both peer
                      recoveries as well as snapshot recoveries (i.e., restores from
                      a snapshot). Default: 40'
                    type: number
                  ismEnabled:
                    description: 'Specifies whether ISM is enabled or not. Default:
                      true'
                    type: boolean
                  ismHistoryEnabled:
                    description: 'Specifies whether audit history is enabled or not.
                      The logs from ISM are automatically indexed to a logs document.
                      Default: true'
                    type: boolean
                  ismHistoryMaxAgeHours:
                    description: 'Maximum age before rolling over the audit history
                      index, in hours. Default: 24'
                    type: number
                  ismHistoryMaxDocs:
                    description: 'Maximum number of documents before rolling over
                      the audit history index. Default: 2500000'
                    type: number
                  ismHistoryRolloverCheckPeriodHours:
                    description: 'The time between rollover checks for the audit history
                      index, in hours. Default: 8'
                    type: number
                  ismHistoryRolloverRetentionPeriodDays:
                    description: 'Length of time long audit history indices are kept,
                      in days. Default: 30'
                    type: number
                  overrideMainResponseVersion:
                    description: 'Compatibility mode sets OpenSearch to report its
                      version as 7.10 so clients continue to work. Default: false'
                    type: boolean
                  pluginsAlertingFilterByBackendRolesEnabled:
                    description: 'Enable or disable filtering of alerting by backend
                      roles. Default: false'
                    type: boolean
                  reindexRemoteWhitelist:
                    description: Allowlist of remote IP addresses for reindexing.
                      Changing this value will cause all OpenSearch instances to restart.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  scriptMaxCompilationsRate:
                    description: Limits the number of inline script compilations within
                      a period of time. Default is use-context
                    type: string
                  searchMaxBuckets:
                    description: 'Maximum number of aggregation buckets allowed in
                      a single response. Default: 10000'
                    type: number
                  threadPoolAnalyzeQueueSize:
                    description: Size of queue for operations in the analyze thread
                      pool.
                    type: number
                  threadPoolAnalyzeSize:
                    description: Number of workers in the analyze operation thread
                      pool. Do note this may have maximum value depending on CPU count
                      - value is automatically lowered if set to higher than maximum
                      value.
                    type: number
                  threadPoolForceMergeSize:
                    description: Number of workers in the force merge operation thread
                      pool. This pool is used for forcing a merge between shards of
                      one or more indices. Do note this may have maximum value depending
                      on CPU count - value is automatically lowered if set to higher
                      than maximum value.
                    type: number
                  threadPoolGetQueueSize:
                    description: Size of queue for operations in the get thread pool.
                    type: number
                  threadPoolGetSize:
                    description: Number of workers in the get operation thread pool.
                      Do note this may have maximum value depending on CPU count -
                      value is automatically lowered if set to higher than maximum
                      value.
                    type: number
                  threadPoolSearchQueueSize:
                    description: Size of queue for operations in the search thread
                      pool.
                    type: number
                  threadPoolSearchSize:
                    description: Number of workers in the search operation thread
                      pool. Do note this may have maximum value depending on CPU count
                      - value is automatically lowered if set to higher than maximum
                      value.
                    type: number
                  threadPoolSearchThrottledQueueSize:
                    description: Size of queue for operations in the search throttled
                      thread pool.
                    type: number
                  threadPoolSearchThrottledSize:
                    description: Number of workers in the search throttled operation
                      thread pool. This pool is used for searching frozen indices.
                      Do note this may have maximum value depending on CPU count -
                      value is automatically lowered if set to higher than maximum
                      value.
                    type: number
                  threadPoolWriteQueueSize:
                    description: Size of queue for operations in the write thread
                      pool.
                    type: number
                  threadPoolWriteSize:
                    description: Number of workers in the write operation thread pool.
                      Do note this may have maximum value depending on CPU count -
                      value is automatically lowered if set to higher than maximum
                      value.
                    type: number
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  actionAutoCreateIndexEnabled:
                    description: 'Specifices whether to allow automatic creation of
                      indices. Default: true'
                    type: boolean
                  actionDestructiveRequiresName:
                    description: Specifies whether to require explicit index names
                      when deleting indices.
                    type: boolean
                  clusterId:
                    description: The ID of the target Opensearch cluster.
                    type: string
                  clusterIdRef:
                    description: Reference to a Cluster in database to populate clusterId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  clusterIdSelector:
                    description: Selector for a Cluster in database to populate clusterId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  clusterMaxShardsPerNode:
                    description: Maximum number of shards allowed per data node.
                    type: number
                  clusterRoutingAllocationNodeConcurrentRecoveries:
                    description: 'Maximum concurrent incoming/outgoing shard recoveries
                      (normally replicas) are allowed to happen per node. Default:
                      2'
                    type: number
                  enableSecurityAudit:
                    description: 'Specifies whether to allow security audit logging.
                      Default: false'
                    type: boolean
                  httpMaxContentLengthBytes:
                    description: 'Maximum content length for HTTP requests to the
                      OpenSearch HTTP API, in bytes. Default: 100000000'
                    type: number
                  httpMaxHeaderSizeBytes:
                    description: 'Maximum size of allowed headers, in bytes. Default:
                      8192'
                    type: number
                  httpMaxInitialLineLengthBytes:
                    description: 'Maximum length of an HTTP URL, in bytes. Default:
                      4096'
                    type: number
                  indicesFielddataCacheSizePercentage:
                    description: Maximum amount of heap memory used for field data
                      cache, expressed as a percentage. Decreasing the value too much
                      will increase overhead of loading field data. Increasing the
                      value too much will decrease amount of heap available for other
                      operations.
                    type: number
                  indicesMemoryIndexBufferSizePercentage:
                    description: 'Total amount of heap used for indexing buffer before
                      writing segments to disk, expressed as a percentage. Too low
                      value will slow down indexing; too high value will increase
                      indexing performance but causes performance issues for query
                      performance. Default: 10'
                    type: number
                  indicesMemoryMaxIndexBufferSizeMb:
                    description: Maximum amount of heap used for indexing buffer before
                      writing segments to disk, in mb. Works in conjunction with indices_memory_index_buffer_size_percentage,
                      each being enforced. The default is unbounded.
                    type: number
                  indicesMemoryMinIndexBufferSizeMb:
                    description: 'Minimum amount of heap used for indexing buffer
                      before writing segments to disk, in mb. Works in conjunction
                      with indices_memory_index_buffer_size_percentage, each being
                      enforced. Default: 48'
                    type: number
                  indicesQueriesCacheSizePercentage:
                    description: 'Maximum amount of heap used for query cache. Too
                      low value will decrease query performance and increase performance
                      for other operations; too high value will cause issues with
                      other functionality. Default: 10'
                    type: number
                  indicesQueryBoolMaxClauseCount:
                    description: 'Maximum number of clauses Lucene BooleanQuery can
                      have. Only increase it if necessary, as it may cause performance
                      issues. Default: 1024'
                    type: number
                  indicesRecoveryMaxConcurrentFileChunks:
                    description: 'Maximum number of file chunks sent in parallel for
                      each recovery. Default: 2'
                    type: number
                  indicesRecoveryMaxMbPerSec:
                    description: 'Limits total inbound and outbound recovery traffic
                      for each node, expressed in mb per second. Applies to both peer
                      recoveries as well as snapshot recoveries (i.e., restores from
                      a snapshot). Default: 40'
                    type: number
                  ismEnabled:
                    description: 'Specifies whether ISM is enabled or not. Default:
                      true'
                    type: boolean
                  ismHistoryEnabled:
                    description: 'Specifies whether audit history is enabled or not.
                      The logs from ISM are automatically indexed to a logs document.
                      Default: true'
                    type: boolean
                  ismHistoryMaxAgeHours:
                    description: 'Maximum age before rolling over the audit history
                      index, in hours. Default: 24'
                    type: number
                  ismHistoryMaxDocs:
                    description: 'Maximum number of documents before rolling over
                      the audit history index. Default: 2500000'
                    type: number
                  ismHistoryRolloverCheckPeriodHours:
                    description: 'The time between rollover checks for the audit history
                      index, in hours. Default: 8'
                    type: number
                  ismHistoryRolloverRetentionPeriodDays:
                    description: 'Length of time long audit history indices are kept,
                      in days. Default: 30'
                    type: number
                  overrideMainResponseVersion:
                    description: 'Compatibility mode sets OpenSearch to report its
                      version as 7.10 so clients continue to work. Default: false'
                    type: boolean
                  pluginsAlertingFilterByBackendRolesEnabled:
                    description: 'Enable or disable filtering of alerting by backend
                      roles. Default: false'
                    type: boolean
                  reindexRemoteWhitelist:
                    description: Allowlist of remote IP addresses for reindexing.
                      Changing this value will cause all OpenSearch instances to restart.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  scriptMaxCompilationsRate:
                    description: Limits the number of inline script compilations within
                      a period of time. Default is use-context
                    type: string
                  searchMaxBuckets:
                    description: 'Maximum number of aggregation buckets allowed in
                      a single response. Default: 10000'
                    type: number
                  threadPoolAnalyzeQueueSize:
                    description: Size of queue for operations in the analyze thread
                      pool.
                    type: number
                  threadPoolAnalyzeSize:
                    description: Number of workers in the analyze operation thread
                      pool. Do note this may have maximum value depending on CPU count
                      - value is automatically lowered if set to higher than maximum
                      value.
                    type: number
                  threadPoolForceMergeSize:
                    description: Number of workers in the force merge operation thread
                      pool. This pool is used for forcing a merge between shards of
                      one or more indices. Do note this may have maximum value depending
                      on CPU count - value is automatically lowered if set to higher
                      than maximum value.
                    type: number
                  threadPoolGetQueueSize:
                    description: Size of queue for operations in the get thread pool.
                    type: number
                  threadPoolGetSize:
                    description: Number of workers in the get operation thread pool.
                      Do note this may have maximum value depending on CPU count -
                      value is automatically lowered if set to higher than maximum
                      value.
                    type: number
                  threadPoolSearchQueueSize:
                    description: Size of queue for operations in the search thread
                      pool.
                    type: number
                  threadPoolSearchSize:
                    description: Number of workers in the search operation thread
                      pool. Do note this may have maximum value depending on CPU count
                      - value is automatically lowered if set to higher than maximum
                      value.
                    type: number
                  threadPoolSearchThrottledQueueSize:
                    description: Size of queue for operations in the search throttled
                      thread pool.
                    type: number
                  threadPoolSearchThrottledSize:
                    description: Number of workers in the search throttled operation
                      thread pool. This pool is used for searching frozen indices.
                      Do note this may have maximum value depending on CPU count -
                      value is automatically lowered if set to higher than maximum
                      value.
                    type: number
                  threadPoolWriteQueueSize:
                    description: Size of queue for operations in the write thread
                      pool.
                    type: number
                  threadPoolWriteSize:
                    description: Number of workers in the write operation thread pool.
                      Do note this may have maximum value depending on CPU count -
                      value is automatically lowered if set to higher than maximum
                      value.
                    type: number
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: OpenSearchConfigStatus defines the observed state of OpenSearchConfig.
            properties:
              atProvider:
                properties:
                  actionAutoCreateIndexEnabled:
                    description: 'Specifices whether to allow automatic creation of
                      indices. Default: true'
                    type: boolean
                  actionDestructiveRequiresName:
                    description: Specifies whether to require explicit index names
                      when deleting indices.
                    type: boolean
                  clusterId:
                    description: The ID of the target Opensearch cluster.
                    type: string
                  clusterMaxShardsPerNode:
                    description: Maximum number of shards allowed per data node.
                    type: number
                  clusterRoutingAllocationNodeConcurrentRecoveries:
                    description: 'Maximum concurrent incoming/outgoing shard recoveries
                      (normally replicas) are allowed to happen per node. Default:
                      2'
                    type: number
                  enableSecurityAudit:
                    description: 'Specifies whether to allow security audit logging.
                      Default: false'
                    type: boolean
                  httpMaxContentLengthBytes:
                    description: 'Maximum content length for HTTP requests to the
                      OpenSearch HTTP API, in bytes. Default: 100000000'
                    type: number
                  httpMaxHeaderSizeBytes:
                    description: 'Maximum size of allowed headers, in bytes. Default:
                      8192'
                    type: number
                  httpMaxInitialLineLengthBytes:
                    description: 'Maximum length of an HTTP URL, in bytes. Default:
                      4096'
                    type: number
                  id:
                    type: string
                  indicesFielddataCacheSizePercentage:
                    description: Maximum amount of heap memory used for field data
                      cache, expressed as a percentage. Decreasing the value too much
                      will increase overhead of loading field data. Increasing the
                      value too much will decrease amount of heap available for other
                      operations.
                    type: number
                  indicesMemoryIndexBufferSizePercentage:
                    description: 'Total amount of heap used for indexing buffer before
                      writing segments to disk, expressed as a percentage. Too low
                      value will slow down indexing; too high value will increase
                      indexing performance but causes performance issues for query
                      performance. Default: 10'
                    type: number
                  indicesMemoryMaxIndexBufferSizeMb:
                    description: Maximum amount of heap used for indexing buffer before
                      writing segments to disk, in mb. Works in conjunction with indices_memory_index_buffer_size_percentage,
                      each being enforced. The default is unbounded.
                    type: number
                  indicesMemoryMinIndexBufferSizeMb:
                    description: 'Minimum amount of heap used for indexing buffer
                      before writing segments to disk, in mb. Works in conjunction
                      with indices_memory_index_buffer_size_percentage, each being
                      enforced. Default: 48'
                    type: number
                  indicesQueriesCacheSizePercentage:
                    description: 'Maximum amount of heap used for query cache. Too
                      low value will decrease query performance and increase performance
                      for other operations; too high value will cause issues with
                      other functionality. Default: 10'
                    type: number
                  indicesQueryBoolMaxClauseCount:
                    description: 'Maximum number of clauses Lucene BooleanQuery can
                      have. Only increase it if necessary, as it may cause performance
                      issues. Default: 1024'
                    type: number
                  indicesRecoveryMaxConcurrentFileChunks:
                    description: 'Maximum number of file chunks sent in parallel for
                      each recovery. Default: 2'
                    type: number
                  indicesRecoveryMaxMbPerSec:
                    description: 'Limits total inbound and outbound recovery traffic
                      for each node, expressed in mb per second. Applies to both peer
                      recoveries as well as snapshot recoveries (i.e., restores from
                      a snapshot). Default: 40'
                    type: number
                  ismEnabled:
                    description: 'Specifies whether ISM is enabled or not. Default:
                      true'
                    type: boolean
                  ismHistoryEnabled:
                    description: 'Specifies whether audit history is enabled or not.
                      The logs from ISM are automatically indexed to a logs document.
                      Default: true'
                    type: boolean
                  ismHistoryMaxAgeHours:
                    description: 'Maximum age before rolling over the audit history
                      index, in hours. Default: 24'
                    type: number
                  ismHistoryMaxDocs:
                    description: 'Maximum number of documents before rolling over
                      the audit history index. Default: 2500000'
                    type: number
                  ismHistoryRolloverCheckPeriodHours:
                    description: 'The time between rollover checks for the audit history
                      index, in hours. Default: 8'
                    type: number
                  ismHistoryRolloverRetentionPeriodDays:
                    description: 'Length of time long audit history indices are kept,
                      in days. Default: 30'
                    type: number
                  overrideMainResponseVersion:
                    description: 'Compatibility mode sets OpenSearch to report its
                      version as 7.10 so clients continue to work. Default: false'
                    type: boolean
                  pluginsAlertingFilterByBackendRolesEnabled:
                    description: 'Enable or disable filtering of alerting by backend
                      roles. Default: false'
                    type: boolean
                  reindexRemoteWhitelist:
                    description: Allowlist of remote IP addresses for reindexing.
                      Changing this value will cause all OpenSearch instances to restart.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  scriptMaxCompilationsRate:
                    description: Limits the number of inline script compilations within
                      a period of time. Default is use-context
                    type: string
                  searchMaxBuckets:
                    description: 'Maximum number of aggregation buckets allowed in
                      a single response. Default: 10000'
                    type: number
                  threadPoolAnalyzeQueueSize:
                    description: Size of queue for operations in the analyze thread
                      pool.
                    type: number
                  threadPoolAnalyzeSize:
                    description: Number of workers in the analyze operation thread
                      pool. Do note this may have maximum value depending on CPU count
                      - value is automatically lowered if set to higher than maximum
                      value.
                    type: number
                  threadPoolForceMergeSize:
                    description: Number of workers in the force merge operation thread
                      pool. This pool is used for forcing a merge between shards of
                      one or more indices. Do note this may have maximum value depending
                      on CPU count - value is automatically lowered if set to higher
                      than maximum value.
                    type: number
                  threadPoolGetQueueSize:
                    description: Size of queue for operations in the get thread pool.
                    type: number
                  threadPoolGetSize:
                    description: Number of workers in the get operation thread pool.
                      Do note this may have maximum value depending on CPU count -
                      value is automatically lowered if set to higher than maximum
                      value.
                    type: number
                  threadPoolSearchQueueSize:
                    description: Size of queue for operations in the search thread
                      pool.
                    type: number
                  threadPoolSearchSize:
                    description: Number of workers in the search operation thread
                      pool. Do note this may have maximum value depending on CPU count
                      - value is automatically lowered if set to higher than maximum
                      value.
                    type: number
                  threadPoolSearchThrottledQueueSize:
                    description: Size of queue for operations in the search throttled
                      thread pool.
                    type: number
                  threadPoolSearchThrottledSize:
                    description: Number of workers in the search throttled operation
                      thread pool. This pool is used for searching frozen indices.
                      Do note this may have maximum value depending on CPU count -
                      value is automatically lowered if set to higher than maximum
                      value.
                    type: number
                  threadPoolWriteQueueSize:
                    description: Size of queue for operations in the write thread
                      pool.
                    type: number
                  threadPoolWriteSize:
                    description: Number of workers in the write operation thread pool.
                      Do note this may have maximum value depending on CPU count -
                      value is automatically lowered if set to higher than maximum
                      value.
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
