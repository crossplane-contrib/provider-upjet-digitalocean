---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: apps.digitalocean.digitalocean.crossplane.io
spec:
  group: digitalocean.digitalocean.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - do
    kind: App
    listKind: AppList
    plural: apps
    singular: app
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: App is the Schema for the Apps API.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: AppSpec defines the desired state of App
            properties:
              deletionPolicy:
                default: Delete
                description: 'DeletionPolicy specifies what will happen to the underlying
                  external when this managed resource is deleted - either "Delete"
                  or "Orphan" the external resource. This field is planned to be deprecated
                  in favor of the ManagementPolicies field in a future release. Currently,
                  both could be set independently and non-default values would be
                  honored if the feature flag is enabled. See the design doc for more
                  information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223'
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  projectId:
                    description: The ID of the project that the app is assigned to.
                    type: string
                  projectIdRef:
                    description: Reference to a Project in digitalocean to populate
                      projectId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: Resolution specifies whether resolution of
                              this reference is required. The default is 'Required',
                              which means the reconcile will fail if the reference
                              cannot be resolved. 'Optional' means this reference
                              will be a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: Resolve specifies when this reference should
                              be resolved. The default is 'IfNotPresent', which will
                              attempt to resolve the reference only when the corresponding
                              field is not present. Use 'Always' to resolve the reference
                              on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  projectIdSelector:
                    description: Selector for a Project in digitalocean to populate
                      projectId.
                    properties:
                      matchControllerRef:
                        description: MatchControllerRef ensures an object with the
                          same controller reference as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: Resolution specifies whether resolution of
                              this reference is required. The default is 'Required',
                              which means the reconcile will fail if the reference
                              cannot be resolved. 'Optional' means this reference
                              will be a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: Resolve specifies when this reference should
                              be resolved. The default is 'IfNotPresent', which will
                              attempt to resolve the reference only when the corresponding
                              field is not present. Use 'Always' to resolve the reference
                              on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  spec:
                    description: A DigitalOcean App spec describing the app. A DigitalOcean
                      App Platform Spec
                    items:
                      properties:
                        alert:
                          description: Describes an alert policy for the app.
                          items:
                            properties:
                              disabled:
                                description: 'Determines whether or not the alert
                                  is disabled (default: false).'
                                type: boolean
                              rule:
                                description: 'The type of the alert to configure.
                                  Top-level app alert policies can be: DEPLOYMENT_FAILED,
                                  DEPLOYMENT_LIVE, DOMAIN_FAILED, or DOMAIN_LIVE.'
                                type: string
                            type: object
                          type: array
                        database:
                          items:
                            properties:
                              clusterName:
                                description: The name of the underlying DigitalOcean
                                  DBaaS cluster. This is required for production databases.
                                  For dev databases, if cluster_name is not set, a
                                  new cluster will be provisioned. The name of the
                                  underlying DigitalOcean DBaaS cluster. This is required
                                  for production databases. For dev databases, if
                                  cluster_name is not set, a new cluster will be provisioned.
                                type: string
                              dbName:
                                description: The name of the MySQL or PostgreSQL database
                                  to configure. The name of the MySQL or PostgreSQL
                                  database to configure.
                                type: string
                              dbUser:
                                description: The name of the MySQL or PostgreSQL user
                                  to configure. The name of the MySQL or PostgreSQL
                                  user to configure.
                                type: string
                              engine:
                                description: The database engine to use (MYSQL, PG,
                                  REDIS, or MONGODB). The database engine to use.
                                type: string
                              name:
                                description: The name of the app. Must be unique across
                                  all apps in the same account. The name of the component
                                type: string
                              production:
                                description: Whether this is a production or dev database.
                                  Whether this is a production or dev database.
                                type: boolean
                              version:
                                description: The version of the database engine. The
                                  version of the database engine.
                                type: string
                            type: object
                          type: array
                        domain:
                          description: Describes a domain where the application will
                            be made available.
                          items:
                            properties:
                              name:
                                description: The name of the app. Must be unique across
                                  all apps in the same account. The hostname for the
                                  domain.
                                type: string
                              type:
                                description: 'The domain type, which can be one of
                                  the following: The type of the domain.'
                                type: string
                              wildcard:
                                description: A boolean indicating whether the domain
                                  includes all sub-domains, in addition to the given
                                  domain. Indicates whether the domain includes all
                                  sub-domains, in addition to the given domain.
                                type: boolean
                              zone:
                                description: If the domain uses DigitalOcean DNS and
                                  you would like App Platform to automatically manage
                                  it for you, set this to the name of the domain on
                                  your account. If the domain uses DigitalOcean DNS
                                  and you would like App Platform to automatically
                                  manage it for you, set this to the name of the domain
                                  on your account.
                                type: string
                            type: object
                          type: array
                        domains:
                          items:
                            type: string
                          type: array
                        env:
                          description: Describes an app-wide environment variable
                            made available to all components.
                          items:
                            properties:
                              key:
                                description: The name of the environment variable.
                                  The name of the environment variable.
                                type: string
                              scope:
                                description: The visibility scope of the environment
                                  variable. One of RUN_TIME, BUILD_TIME, or RUN_AND_BUILD_TIME
                                  (default). The visibility scope of the environment
                                  variable.
                                type: string
                              type:
                                description: 'The domain type, which can be one of
                                  the following: The type of the environment variable.'
                                type: string
                              valueSecretRef:
                                description: The value of the environment variable.
                                  The value of the environment variable.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                            type: object
                          type: array
                        features:
                          description: A list of the features applied to the app.
                            The default buildpack can be overridden here. List of
                            available buildpacks can be found using the doctl CLI
                            List of features which is applied to the app
                          items:
                            type: string
                          type: array
                        function:
                          items:
                            properties:
                              alert:
                                description: Describes an alert policy for the app.
                                  Alert policies for the app component
                                items:
                                  properties:
                                    disabled:
                                      description: 'Determines whether or not the
                                        alert is disabled (default: false).'
                                      type: boolean
                                    operator:
                                      description: The operator to use. This is either
                                        of GREATER_THAN or LESS_THAN.
                                      type: string
                                    rule:
                                      description: 'The type of the alert to configure.
                                        Top-level app alert policies can be: DEPLOYMENT_FAILED,
                                        DEPLOYMENT_LIVE, DOMAIN_FAILED, or DOMAIN_LIVE.'
                                      type: string
                                    value:
                                      description: The value of the environment variable.
                                      type: number
                                    window:
                                      description: 'The time before alerts should
                                        be triggered. This is may be one of: FIVE_MINUTES,
                                        TEN_MINUTES, THIRTY_MINUTES, ONE_HOUR.'
                                      type: string
                                  type: object
                                type: array
                              cors:
                                description: The CORS policies of the app.
                                items:
                                  properties:
                                    allowCredentials:
                                      description: Whether browsers should expose
                                        the response to the client-side JavaScript
                                        code when the request's credentials mode is
                                        include. This configures the Access-Control-Allow-Credentials
                                        header. Whether browsers should expose the
                                        response to the client-side JavaScript code
                                        when the request’s credentials mode is `include`.
                                        This configures the Access-Control-Allow-Credentials
                                        header.
                                      type: boolean
                                    allowHeaders:
                                      description: The set of allowed HTTP request
                                        headers. This configures the Access-Control-Allow-Headers
                                        header. The set of allowed HTTP request headers.
                                        This configures the Access-Control-Allow-Headers
                                        header.
                                      items:
                                        type: string
                                      type: array
                                    allowMethods:
                                      description: The set of allowed HTTP methods.
                                        This configures the Access-Control-Allow-Methods
                                        header. The set of allowed HTTP methods. This
                                        configures the Access-Control-Allow-Methods
                                        header.
                                      items:
                                        type: string
                                      type: array
                                    allowOrigins:
                                      description: The Access-Control-Allow-Origin
                                        can be The set of allowed CORS origins. This
                                        configures the Access-Control-Allow-Origin
                                        header.
                                      items:
                                        properties:
                                          exact:
                                            description: The Access-Control-Allow-Origin
                                              header will be set to the client's origin
                                              only if the client's origin exactly
                                              matches the value you provide. Exact
                                              string match.
                                            type: string
                                          prefix:
                                            description: Prefix-based match. Prefix-based
                                              match.
                                            type: string
                                          regex:
                                            description: The Access-Control-Allow-Origin
                                              header will be set to the client's origin
                                              if the client’s origin matches the regex
                                              you provide, in RE2 style syntax. RE2
                                              style regex-based match.
                                            type: string
                                        type: object
                                      type: array
                                    exposeHeaders:
                                      description: The set of HTTP response headers
                                        that browsers are allowed to access. This
                                        configures the Access-Control-Expose-Headers
                                        header. The set of HTTP response headers that
                                        browsers are allowed to access. This configures
                                        the Access-Control-Expose-Headers header.
                                      items:
                                        type: string
                                      type: array
                                    maxAge:
                                      description: 'An optional duration specifying
                                        how long browsers can cache the results of
                                        a preflight request. This configures the Access-Control-Max-Age
                                        header. Example: 5h30m. An optional duration
                                        specifying how long browsers can cache the
                                        results of a preflight request. This configures
                                        the Access-Control-Max-Age header. Example:
                                        `5h30m`.'
                                      type: string
                                  type: object
                                type: array
                              env:
                                description: Describes an app-wide environment variable
                                  made available to all components.
                                items:
                                  properties:
                                    key:
                                      description: The name of the environment variable.
                                        The name of the environment variable.
                                      type: string
                                    scope:
                                      description: The visibility scope of the environment
                                        variable. One of RUN_TIME, BUILD_TIME, or
                                        RUN_AND_BUILD_TIME (default). The visibility
                                        scope of the environment variable.
                                      type: string
                                    type:
                                      description: 'The domain type, which can be
                                        one of the following: The type of the environment
                                        variable.'
                                      type: string
                                    valueSecretRef:
                                      description: The value of the environment variable.
                                        The value of the environment variable.
                                      properties:
                                        key:
                                          description: The key to select.
                                          type: string
                                        name:
                                          description: Name of the secret.
                                          type: string
                                        namespace:
                                          description: Namespace of the secret.
                                          type: string
                                      required:
                                      - key
                                      - name
                                      - namespace
                                      type: object
                                  type: object
                                type: array
                              git:
                                description: A Git repo to use as the component's
                                  source. The repository must be able to be cloned
                                  without authentication.  Only one of git, github
                                  or gitlab  may be set
                                items:
                                  properties:
                                    branch:
                                      description: The name of the branch to use.
                                        The name of the branch to use.
                                      type: string
                                    repoCloneUrl:
                                      description: The clone URL of the repo. The
                                        clone URL of the repo.
                                      type: string
                                  type: object
                                type: array
                              github:
                                description: A GitHub repo to use as the component's
                                  source. DigitalOcean App Platform must have access
                                  to the repository. Only one of git, github, gitlab,
                                  or image may be set.
                                items:
                                  properties:
                                    branch:
                                      description: The name of the branch to use.
                                        The name of the branch to use.
                                      type: string
                                    deployOnPush:
                                      description: Whether to automatically deploy
                                        new commits made to the repo. Whether to automatically
                                        deploy new commits made to the repo
                                      type: boolean
                                    repo:
                                      description: The name of the repo in the format
                                        owner/repo. The name of the repo in the format
                                        `owner/repo`.
                                      type: string
                                  type: object
                                type: array
                              gitlab:
                                description: A Gitlab repo to use as the component's
                                  source. DigitalOcean App Platform must have access
                                  to the repository. Only one of git, github, gitlab,
                                  or image may be set.
                                items:
                                  properties:
                                    branch:
                                      description: The name of the branch to use.
                                        The name of the branch to use.
                                      type: string
                                    deployOnPush:
                                      description: Whether to automatically deploy
                                        new commits made to the repo. Whether to automatically
                                        deploy new commits made to the repo
                                      type: boolean
                                    repo:
                                      description: The name of the repo in the format
                                        owner/repo. The name of the repo in the format
                                        `owner/repo`.
                                      type: string
                                  type: object
                                type: array
                              logDestination:
                                description: Describes a log forwarding destination.
                                  Logs
                                items:
                                  properties:
                                    datadog:
                                      description: Datadog configuration. Datadog
                                        configuration.
                                      items:
                                        properties:
                                          apiKey:
                                            description: Datadog API key. Datadog
                                              API key.
                                            type: string
                                          endpoint:
                                            description: Papertrail syslog endpoint.
                                              Datadog HTTP log intake endpoint.
                                            type: string
                                        type: object
                                      type: array
                                    logtail:
                                      description: Logtail configuration. Logtail
                                        configuration.
                                      items:
                                        properties:
                                          token:
                                            description: Logtail token. Logtail token.
                                            type: string
                                        type: object
                                      type: array
                                    name:
                                      description: The name of the app. Must be unique
                                        across all apps in the same account. Name
                                        of the log destination
                                      type: string
                                    papertrail:
                                      description: Papertrail configuration. Papertrail
                                        configuration.
                                      items:
                                        properties:
                                          endpoint:
                                            description: Papertrail syslog endpoint.
                                              Papertrail syslog endpoint.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              name:
                                description: The name of the app. Must be unique across
                                  all apps in the same account. The name of the component
                                type: string
                              routes:
                                description: (Deprecated - use ingress) An HTTP paths
                                  that should be routed to this component.
                                items:
                                  properties:
                                    path:
                                      description: The path to match on. Path specifies
                                        an route by HTTP path prefix. Paths must start
                                        with / and must be unique within the app.
                                      type: string
                                    preservePathPrefix:
                                      description: An optional boolean flag to preserve
                                        the path that is forwarded to the backend
                                        service. By default, the HTTP request path
                                        will be trimmed from the left when forwarded
                                        to the component. An optional flag to preserve
                                        the path that is forwarded to the backend
                                        service.
                                      type: boolean
                                  type: object
                                type: array
                              sourceDir:
                                description: An optional path to the working directory
                                  to use for the build. An optional path to the working
                                  directory to use for the build.
                                type: string
                            type: object
                          type: array
                        ingress:
                          description: Specification for component routing, rewrites,
                            and redirects.
                          items:
                            properties:
                              rule:
                                description: 'The type of the alert to configure.
                                  Top-level app alert policies can be: DEPLOYMENT_FAILED,
                                  DEPLOYMENT_LIVE, DOMAIN_FAILED, or DOMAIN_LIVE.'
                                items:
                                  properties:
                                    component:
                                      description: The component to route to. Only
                                        one of component or redirect may be set.
                                      items:
                                        properties:
                                          name:
                                            description: The name of the app. Must
                                              be unique across all apps in the same
                                              account.
                                            type: string
                                          preservePathPrefix:
                                            description: An optional boolean flag
                                              to preserve the path that is forwarded
                                              to the backend service. By default,
                                              the HTTP request path will be trimmed
                                              from the left when forwarded to the
                                              component.
                                            type: boolean
                                          rewrite:
                                            description: An optional field that will
                                              rewrite the path of the component to
                                              be what is specified here. This is mutually
                                              exclusive with preserve_path_prefix.
                                            type: string
                                        type: object
                                      type: array
                                    cors:
                                      description: The CORS policies of the app.
                                      items:
                                        properties:
                                          allowCredentials:
                                            description: Whether browsers should expose
                                              the response to the client-side JavaScript
                                              code when the request's credentials
                                              mode is include. This configures the
                                              Access-Control-Allow-Credentials header.
                                              Whether browsers should expose the response
                                              to the client-side JavaScript code when
                                              the request’s credentials mode is `include`.
                                              This configures the Access-Control-Allow-Credentials
                                              header.
                                            type: boolean
                                          allowHeaders:
                                            description: The set of allowed HTTP request
                                              headers. This configures the Access-Control-Allow-Headers
                                              header. The set of allowed HTTP request
                                              headers. This configures the Access-Control-Allow-Headers
                                              header.
                                            items:
                                              type: string
                                            type: array
                                          allowMethods:
                                            description: The set of allowed HTTP methods.
                                              This configures the Access-Control-Allow-Methods
                                              header. The set of allowed HTTP methods.
                                              This configures the Access-Control-Allow-Methods
                                              header.
                                            items:
                                              type: string
                                            type: array
                                          allowOrigins:
                                            description: The Access-Control-Allow-Origin
                                              can be The set of allowed CORS origins.
                                              This configures the Access-Control-Allow-Origin
                                              header.
                                            items:
                                              properties:
                                                exact:
                                                  description: The Access-Control-Allow-Origin
                                                    header will be set to the client's
                                                    origin only if the client's origin
                                                    exactly matches the value you
                                                    provide. Exact string match.
                                                  type: string
                                                prefix:
                                                  description: Prefix-based match.
                                                    Prefix-based match.
                                                  type: string
                                                regex:
                                                  description: The Access-Control-Allow-Origin
                                                    header will be set to the client's
                                                    origin if the client’s origin
                                                    matches the regex you provide,
                                                    in RE2 style syntax. RE2 style
                                                    regex-based match.
                                                  type: string
                                              type: object
                                            type: array
                                          exposeHeaders:
                                            description: The set of HTTP response
                                              headers that browsers are allowed to
                                              access. This configures the Access-Control-Expose-Headers
                                              header. The set of HTTP response headers
                                              that browsers are allowed to access.
                                              This configures the Access-Control-Expose-Headers
                                              header.
                                            items:
                                              type: string
                                            type: array
                                          maxAge:
                                            description: 'An optional duration specifying
                                              how long browsers can cache the results
                                              of a preflight request. This configures
                                              the Access-Control-Max-Age header. Example:
                                              5h30m. An optional duration specifying
                                              how long browsers can cache the results
                                              of a preflight request. This configures
                                              the Access-Control-Max-Age header. Example:
                                              `5h30m`.'
                                            type: string
                                        type: object
                                      type: array
                                    match:
                                      description: The match configuration for the
                                        rule
                                      items:
                                        properties:
                                          path:
                                            description: The path to match on.
                                            items:
                                              properties:
                                                prefix:
                                                  description: Prefix-based match.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    redirect:
                                      description: The redirect configuration for
                                        the rule. Only one of component or redirect
                                        may be set.
                                      items:
                                        properties:
                                          authority:
                                            description: The authority/host to redirect
                                              to. This can be a hostname or IP address.
                                            type: string
                                          port:
                                            description: The port to redirect to.
                                            type: number
                                          redirectCode:
                                            description: The redirect code to use.
                                              Supported values are 300, 301, 302,
                                              303, 304, 307, 308.
                                            type: number
                                          scheme:
                                            description: The scheme to redirect to.
                                              Supported values are http or https
                                            type: string
                                          uri:
                                            description: An optional URI path to redirect
                                              to.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                            type: object
                          type: array
                        job:
                          items:
                            properties:
                              alert:
                                description: Describes an alert policy for the app.
                                  Alert policies for the app component
                                items:
                                  properties:
                                    disabled:
                                      description: 'Determines whether or not the
                                        alert is disabled (default: false).'
                                      type: boolean
                                    operator:
                                      description: The operator to use. This is either
                                        of GREATER_THAN or LESS_THAN.
                                      type: string
                                    rule:
                                      description: 'The type of the alert to configure.
                                        Top-level app alert policies can be: DEPLOYMENT_FAILED,
                                        DEPLOYMENT_LIVE, DOMAIN_FAILED, or DOMAIN_LIVE.'
                                      type: string
                                    value:
                                      description: The value of the environment variable.
                                      type: number
                                    window:
                                      description: 'The time before alerts should
                                        be triggered. This is may be one of: FIVE_MINUTES,
                                        TEN_MINUTES, THIRTY_MINUTES, ONE_HOUR.'
                                      type: string
                                  type: object
                                type: array
                              buildCommand:
                                description: An optional build command to run while
                                  building this component from source. An optional
                                  build command to run while building this component
                                  from source.
                                type: string
                              dockerfilePath:
                                description: The path to a Dockerfile relative to
                                  the root of the repo. If set, overrides usage of
                                  buildpacks. The path to a Dockerfile relative to
                                  the root of the repo. If set, overrides usage of
                                  buildpacks.
                                type: string
                              env:
                                description: Describes an app-wide environment variable
                                  made available to all components.
                                items:
                                  properties:
                                    key:
                                      description: The name of the environment variable.
                                        The name of the environment variable.
                                      type: string
                                    scope:
                                      description: The visibility scope of the environment
                                        variable. One of RUN_TIME, BUILD_TIME, or
                                        RUN_AND_BUILD_TIME (default). The visibility
                                        scope of the environment variable.
                                      type: string
                                    type:
                                      description: 'The domain type, which can be
                                        one of the following: The type of the environment
                                        variable.'
                                      type: string
                                    valueSecretRef:
                                      description: The value of the environment variable.
                                        The value of the environment variable.
                                      properties:
                                        key:
                                          description: The key to select.
                                          type: string
                                        name:
                                          description: Name of the secret.
                                          type: string
                                        namespace:
                                          description: Namespace of the secret.
                                          type: string
                                      required:
                                      - key
                                      - name
                                      - namespace
                                      type: object
                                  type: object
                                type: array
                              environmentSlug:
                                description: An environment slug describing the type
                                  of this app. An environment slug describing the
                                  type of this app.
                                type: string
                              git:
                                description: A Git repo to use as the component's
                                  source. The repository must be able to be cloned
                                  without authentication.  Only one of git, github
                                  or gitlab  may be set
                                items:
                                  properties:
                                    branch:
                                      description: The name of the branch to use.
                                        The name of the branch to use.
                                      type: string
                                    repoCloneUrl:
                                      description: The clone URL of the repo. The
                                        clone URL of the repo.
                                      type: string
                                  type: object
                                type: array
                              github:
                                description: A GitHub repo to use as the component's
                                  source. DigitalOcean App Platform must have access
                                  to the repository. Only one of git, github, gitlab,
                                  or image may be set.
                                items:
                                  properties:
                                    branch:
                                      description: The name of the branch to use.
                                        The name of the branch to use.
                                      type: string
                                    deployOnPush:
                                      description: Whether to automatically deploy
                                        new commits made to the repo. Whether to automatically
                                        deploy new commits made to the repo
                                      type: boolean
                                    repo:
                                      description: The name of the repo in the format
                                        owner/repo. The name of the repo in the format
                                        `owner/repo`.
                                      type: string
                                  type: object
                                type: array
                              gitlab:
                                description: A Gitlab repo to use as the component's
                                  source. DigitalOcean App Platform must have access
                                  to the repository. Only one of git, github, gitlab,
                                  or image may be set.
                                items:
                                  properties:
                                    branch:
                                      description: The name of the branch to use.
                                        The name of the branch to use.
                                      type: string
                                    deployOnPush:
                                      description: Whether to automatically deploy
                                        new commits made to the repo. Whether to automatically
                                        deploy new commits made to the repo
                                      type: boolean
                                    repo:
                                      description: The name of the repo in the format
                                        owner/repo. The name of the repo in the format
                                        `owner/repo`.
                                      type: string
                                  type: object
                                type: array
                              image:
                                description: An image to use as the component's source.
                                  Only one of git, github, gitlab, or image may be
                                  set.
                                items:
                                  properties:
                                    deployOnPush:
                                      description: Whether to automatically deploy
                                        new commits made to the repo. Configures automatically
                                        deploying images pushed to DOCR.
                                      items:
                                        properties:
                                          enabled:
                                            description: Whether to automatically
                                              deploy images pushed to DOCR. Whether
                                              to automatically deploy images pushed
                                              to DOCR.
                                            type: boolean
                                        type: object
                                      type: array
                                    registry:
                                      description: The registry name. Must be left
                                        empty for the DOCR registry type. Required
                                        for the DOCKER_HUB registry type. The registry
                                        name. Must be left empty for the DOCR registry
                                        type.
                                      type: string
                                    registryType:
                                      description: The registry type. One of DOCR
                                        (DigitalOcean container registry) or DOCKER_HUB.
                                        The registry type.
                                      type: string
                                    repository:
                                      description: The repository name. The repository
                                        name.
                                      type: string
                                    tag:
                                      description: The repository tag. Defaults to
                                        latest if not provided. The repository tag.
                                        Defaults to latest if not provided.
                                      type: string
                                  type: object
                                type: array
                              instanceCount:
                                description: The amount of instances that this component
                                  should be scaled to. The amount of instances that
                                  this component should be scaled to.
                                type: number
                              instanceSizeSlug:
                                description: 'The instance size to use for this component.
                                  This determines the plan (basic or professional)
                                  and the available CPU and memory. The list of available
                                  instance sizes can be found with the API or using
                                  the doctl CLI (doctl apps tier instance-size list).
                                  Default: basic-xxs The instance size to use for
                                  this component.'
                                type: string
                              kind:
                                description: 'The type of job and when it will be
                                  run during the deployment process. It may be one
                                  of: The type of job and when it will be run during
                                  the deployment process.'
                                type: string
                              logDestination:
                                description: Describes a log forwarding destination.
                                  Logs
                                items:
                                  properties:
                                    datadog:
                                      description: Datadog configuration. Datadog
                                        configuration.
                                      items:
                                        properties:
                                          apiKey:
                                            description: Datadog API key. Datadog
                                              API key.
                                            type: string
                                          endpoint:
                                            description: Papertrail syslog endpoint.
                                              Datadog HTTP log intake endpoint.
                                            type: string
                                        type: object
                                      type: array
                                    logtail:
                                      description: Logtail configuration. Logtail
                                        configuration.
                                      items:
                                        properties:
                                          token:
                                            description: Logtail token. Logtail token.
                                            type: string
                                        type: object
                                      type: array
                                    name:
                                      description: The name of the app. Must be unique
                                        across all apps in the same account. Name
                                        of the log destination
                                      type: string
                                    papertrail:
                                      description: Papertrail configuration. Papertrail
                                        configuration.
                                      items:
                                        properties:
                                          endpoint:
                                            description: Papertrail syslog endpoint.
                                              Papertrail syslog endpoint.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              name:
                                description: The name of the app. Must be unique across
                                  all apps in the same account. The name of the component
                                type: string
                              runCommand:
                                description: An optional run command to override the
                                  component's default. An optional run command to
                                  override the component's default.
                                type: string
                              sourceDir:
                                description: An optional path to the working directory
                                  to use for the build. An optional path to the working
                                  directory to use for the build.
                                type: string
                            type: object
                          type: array
                        name:
                          description: The name of the app. Must be unique across
                            all apps in the same account. The name of the app. Must
                            be unique across all apps in the same account.
                          type: string
                        region:
                          description: The slug for the DigitalOcean data center region
                            hosting the app. The slug for the DigitalOcean data center
                            region hosting the app
                          type: string
                        service:
                          items:
                            properties:
                              alert:
                                description: Describes an alert policy for the app.
                                  Alert policies for the app component
                                items:
                                  properties:
                                    disabled:
                                      description: 'Determines whether or not the
                                        alert is disabled (default: false).'
                                      type: boolean
                                    operator:
                                      description: The operator to use. This is either
                                        of GREATER_THAN or LESS_THAN.
                                      type: string
                                    rule:
                                      description: 'The type of the alert to configure.
                                        Top-level app alert policies can be: DEPLOYMENT_FAILED,
                                        DEPLOYMENT_LIVE, DOMAIN_FAILED, or DOMAIN_LIVE.'
                                      type: string
                                    value:
                                      description: The value of the environment variable.
                                      type: number
                                    window:
                                      description: 'The time before alerts should
                                        be triggered. This is may be one of: FIVE_MINUTES,
                                        TEN_MINUTES, THIRTY_MINUTES, ONE_HOUR.'
                                      type: string
                                  type: object
                                type: array
                              buildCommand:
                                description: An optional build command to run while
                                  building this component from source. An optional
                                  build command to run while building this component
                                  from source.
                                type: string
                              cors:
                                description: The CORS policies of the app.
                                items:
                                  properties:
                                    allowCredentials:
                                      description: Whether browsers should expose
                                        the response to the client-side JavaScript
                                        code when the request's credentials mode is
                                        include. This configures the Access-Control-Allow-Credentials
                                        header. Whether browsers should expose the
                                        response to the client-side JavaScript code
                                        when the request’s credentials mode is `include`.
                                        This configures the Access-Control-Allow-Credentials
                                        header.
                                      type: boolean
                                    allowHeaders:
                                      description: The set of allowed HTTP request
                                        headers. This configures the Access-Control-Allow-Headers
                                        header. The set of allowed HTTP request headers.
                                        This configures the Access-Control-Allow-Headers
                                        header.
                                      items:
                                        type: string
                                      type: array
                                    allowMethods:
                                      description: The set of allowed HTTP methods.
                                        This configures the Access-Control-Allow-Methods
                                        header. The set of allowed HTTP methods. This
                                        configures the Access-Control-Allow-Methods
                                        header.
                                      items:
                                        type: string
                                      type: array
                                    allowOrigins:
                                      description: The Access-Control-Allow-Origin
                                        can be The set of allowed CORS origins. This
                                        configures the Access-Control-Allow-Origin
                                        header.
                                      items:
                                        properties:
                                          exact:
                                            description: The Access-Control-Allow-Origin
                                              header will be set to the client's origin
                                              only if the client's origin exactly
                                              matches the value you provide. Exact
                                              string match.
                                            type: string
                                          prefix:
                                            description: Prefix-based match. Prefix-based
                                              match.
                                            type: string
                                          regex:
                                            description: The Access-Control-Allow-Origin
                                              header will be set to the client's origin
                                              if the client’s origin matches the regex
                                              you provide, in RE2 style syntax. RE2
                                              style regex-based match.
                                            type: string
                                        type: object
                                      type: array
                                    exposeHeaders:
                                      description: The set of HTTP response headers
                                        that browsers are allowed to access. This
                                        configures the Access-Control-Expose-Headers
                                        header. The set of HTTP response headers that
                                        browsers are allowed to access. This configures
                                        the Access-Control-Expose-Headers header.
                                      items:
                                        type: string
                                      type: array
                                    maxAge:
                                      description: 'An optional duration specifying
                                        how long browsers can cache the results of
                                        a preflight request. This configures the Access-Control-Max-Age
                                        header. Example: 5h30m. An optional duration
                                        specifying how long browsers can cache the
                                        results of a preflight request. This configures
                                        the Access-Control-Max-Age header. Example:
                                        `5h30m`.'
                                      type: string
                                  type: object
                                type: array
                              dockerfilePath:
                                description: The path to a Dockerfile relative to
                                  the root of the repo. If set, overrides usage of
                                  buildpacks. The path to a Dockerfile relative to
                                  the root of the repo. If set, overrides usage of
                                  buildpacks.
                                type: string
                              env:
                                description: Describes an app-wide environment variable
                                  made available to all components.
                                items:
                                  properties:
                                    key:
                                      description: The name of the environment variable.
                                        The name of the environment variable.
                                      type: string
                                    scope:
                                      description: The visibility scope of the environment
                                        variable. One of RUN_TIME, BUILD_TIME, or
                                        RUN_AND_BUILD_TIME (default). The visibility
                                        scope of the environment variable.
                                      type: string
                                    type:
                                      description: 'The domain type, which can be
                                        one of the following: The type of the environment
                                        variable.'
                                      type: string
                                    valueSecretRef:
                                      description: The value of the environment variable.
                                        The value of the environment variable.
                                      properties:
                                        key:
                                          description: The key to select.
                                          type: string
                                        name:
                                          description: Name of the secret.
                                          type: string
                                        namespace:
                                          description: Namespace of the secret.
                                          type: string
                                      required:
                                      - key
                                      - name
                                      - namespace
                                      type: object
                                  type: object
                                type: array
                              environmentSlug:
                                description: An environment slug describing the type
                                  of this app. An environment slug describing the
                                  type of this app.
                                type: string
                              git:
                                description: A Git repo to use as the component's
                                  source. The repository must be able to be cloned
                                  without authentication.  Only one of git, github
                                  or gitlab  may be set
                                items:
                                  properties:
                                    branch:
                                      description: The name of the branch to use.
                                        The name of the branch to use.
                                      type: string
                                    repoCloneUrl:
                                      description: The clone URL of the repo. The
                                        clone URL of the repo.
                                      type: string
                                  type: object
                                type: array
                              github:
                                description: A GitHub repo to use as the component's
                                  source. DigitalOcean App Platform must have access
                                  to the repository. Only one of git, github, gitlab,
                                  or image may be set.
                                items:
                                  properties:
                                    branch:
                                      description: The name of the branch to use.
                                        The name of the branch to use.
                                      type: string
                                    deployOnPush:
                                      description: Whether to automatically deploy
                                        new commits made to the repo. Whether to automatically
                                        deploy new commits made to the repo
                                      type: boolean
                                    repo:
                                      description: The name of the repo in the format
                                        owner/repo. The name of the repo in the format
                                        `owner/repo`.
                                      type: string
                                  type: object
                                type: array
                              gitlab:
                                description: A Gitlab repo to use as the component's
                                  source. DigitalOcean App Platform must have access
                                  to the repository. Only one of git, github, gitlab,
                                  or image may be set.
                                items:
                                  properties:
                                    branch:
                                      description: The name of the branch to use.
                                        The name of the branch to use.
                                      type: string
                                    deployOnPush:
                                      description: Whether to automatically deploy
                                        new commits made to the repo. Whether to automatically
                                        deploy new commits made to the repo
                                      type: boolean
                                    repo:
                                      description: The name of the repo in the format
                                        owner/repo. The name of the repo in the format
                                        `owner/repo`.
                                      type: string
                                  type: object
                                type: array
                              healthCheck:
                                description: A health check to determine the availability
                                  of this component.
                                items:
                                  properties:
                                    failureThreshold:
                                      description: The number of failed health checks
                                        before considered unhealthy. The number of
                                        failed health checks before considered unhealthy.
                                      type: number
                                    httpPath:
                                      description: The route path used for the HTTP
                                        health check ping. The route path used for
                                        the HTTP health check ping.
                                      type: string
                                    initialDelaySeconds:
                                      description: The number of seconds to wait before
                                        beginning health checks. The number of seconds
                                        to wait before beginning health checks.
                                      type: number
                                    periodSeconds:
                                      description: The number of seconds to wait between
                                        health checks. The number of seconds to wait
                                        between health checks.
                                      type: number
                                    port:
                                      description: The port to redirect to. The port
                                        on which the health check will be performed.
                                        If not set, the health check will be performed
                                        on the component's http_port.
                                      type: number
                                    successThreshold:
                                      description: The number of successful health
                                        checks before considered healthy. The number
                                        of successful health checks before considered
                                        healthy.
                                      type: number
                                    timeoutSeconds:
                                      description: The number of seconds after which
                                        the check times out. The number of seconds
                                        after which the check times out.
                                      type: number
                                  type: object
                                type: array
                              httpPort:
                                description: The internal port on which this service's
                                  run command will listen. The internal port on which
                                  this service's run command will listen.
                                type: number
                              image:
                                description: An image to use as the component's source.
                                  Only one of git, github, gitlab, or image may be
                                  set.
                                items:
                                  properties:
                                    deployOnPush:
                                      description: Whether to automatically deploy
                                        new commits made to the repo. Configures automatically
                                        deploying images pushed to DOCR.
                                      items:
                                        properties:
                                          enabled:
                                            description: Whether to automatically
                                              deploy images pushed to DOCR. Whether
                                              to automatically deploy images pushed
                                              to DOCR.
                                            type: boolean
                                        type: object
                                      type: array
                                    registry:
                                      description: The registry name. Must be left
                                        empty for the DOCR registry type. Required
                                        for the DOCKER_HUB registry type. The registry
                                        name. Must be left empty for the DOCR registry
                                        type.
                                      type: string
                                    registryType:
                                      description: The registry type. One of DOCR
                                        (DigitalOcean container registry) or DOCKER_HUB.
                                        The registry type.
                                      type: string
                                    repository:
                                      description: The repository name. The repository
                                        name.
                                      type: string
                                    tag:
                                      description: The repository tag. Defaults to
                                        latest if not provided. The repository tag.
                                        Defaults to latest if not provided.
                                      type: string
                                  type: object
                                type: array
                              instanceCount:
                                description: The amount of instances that this component
                                  should be scaled to. The amount of instances that
                                  this component should be scaled to.
                                type: number
                              instanceSizeSlug:
                                description: 'The instance size to use for this component.
                                  This determines the plan (basic or professional)
                                  and the available CPU and memory. The list of available
                                  instance sizes can be found with the API or using
                                  the doctl CLI (doctl apps tier instance-size list).
                                  Default: basic-xxs The instance size to use for
                                  this component.'
                                type: string
                              internalPorts:
                                description: A list of ports on which this service
                                  will listen for internal traffic.
                                items:
                                  type: number
                                type: array
                              logDestination:
                                description: Describes a log forwarding destination.
                                  Logs
                                items:
                                  properties:
                                    datadog:
                                      description: Datadog configuration. Datadog
                                        configuration.
                                      items:
                                        properties:
                                          apiKey:
                                            description: Datadog API key. Datadog
                                              API key.
                                            type: string
                                          endpoint:
                                            description: Papertrail syslog endpoint.
                                              Datadog HTTP log intake endpoint.
                                            type: string
                                        type: object
                                      type: array
                                    logtail:
                                      description: Logtail configuration. Logtail
                                        configuration.
                                      items:
                                        properties:
                                          token:
                                            description: Logtail token. Logtail token.
                                            type: string
                                        type: object
                                      type: array
                                    name:
                                      description: The name of the app. Must be unique
                                        across all apps in the same account. Name
                                        of the log destination
                                      type: string
                                    papertrail:
                                      description: Papertrail configuration. Papertrail
                                        configuration.
                                      items:
                                        properties:
                                          endpoint:
                                            description: Papertrail syslog endpoint.
                                              Papertrail syslog endpoint.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              name:
                                description: The name of the app. Must be unique across
                                  all apps in the same account. The name of the component
                                type: string
                              routes:
                                description: (Deprecated - use ingress) An HTTP paths
                                  that should be routed to this component.
                                items:
                                  properties:
                                    path:
                                      description: The path to match on. Path specifies
                                        an route by HTTP path prefix. Paths must start
                                        with / and must be unique within the app.
                                      type: string
                                    preservePathPrefix:
                                      description: An optional boolean flag to preserve
                                        the path that is forwarded to the backend
                                        service. By default, the HTTP request path
                                        will be trimmed from the left when forwarded
                                        to the component. An optional flag to preserve
                                        the path that is forwarded to the backend
                                        service.
                                      type: boolean
                                  type: object
                                type: array
                              runCommand:
                                description: An optional run command to override the
                                  component's default. An optional run command to
                                  override the component's default.
                                type: string
                              sourceDir:
                                description: An optional path to the working directory
                                  to use for the build. An optional path to the working
                                  directory to use for the build.
                                type: string
                            type: object
                          type: array
                        staticSite:
                          items:
                            properties:
                              buildCommand:
                                description: An optional build command to run while
                                  building this component from source. An optional
                                  build command to run while building this component
                                  from source.
                                type: string
                              catchallDocument:
                                description: The name of the document to use as the
                                  fallback for any requests to documents that are
                                  not found when serving this static site. The name
                                  of the document to use as the fallback for any requests
                                  to documents that are not found when serving this
                                  static site.
                                type: string
                              cors:
                                description: The CORS policies of the app.
                                items:
                                  properties:
                                    allowCredentials:
                                      description: Whether browsers should expose
                                        the response to the client-side JavaScript
                                        code when the request's credentials mode is
                                        include. This configures the Access-Control-Allow-Credentials
                                        header. Whether browsers should expose the
                                        response to the client-side JavaScript code
                                        when the request’s credentials mode is `include`.
                                        This configures the Access-Control-Allow-Credentials
                                        header.
                                      type: boolean
                                    allowHeaders:
                                      description: The set of allowed HTTP request
                                        headers. This configures the Access-Control-Allow-Headers
                                        header. The set of allowed HTTP request headers.
                                        This configures the Access-Control-Allow-Headers
                                        header.
                                      items:
                                        type: string
                                      type: array
                                    allowMethods:
                                      description: The set of allowed HTTP methods.
                                        This configures the Access-Control-Allow-Methods
                                        header. The set of allowed HTTP methods. This
                                        configures the Access-Control-Allow-Methods
                                        header.
                                      items:
                                        type: string
                                      type: array
                                    allowOrigins:
                                      description: The Access-Control-Allow-Origin
                                        can be The set of allowed CORS origins. This
                                        configures the Access-Control-Allow-Origin
                                        header.
                                      items:
                                        properties:
                                          exact:
                                            description: The Access-Control-Allow-Origin
                                              header will be set to the client's origin
                                              only if the client's origin exactly
                                              matches the value you provide. Exact
                                              string match.
                                            type: string
                                          prefix:
                                            description: Prefix-based match. Prefix-based
                                              match.
                                            type: string
                                          regex:
                                            description: The Access-Control-Allow-Origin
                                              header will be set to the client's origin
                                              if the client’s origin matches the regex
                                              you provide, in RE2 style syntax. RE2
                                              style regex-based match.
                                            type: string
                                        type: object
                                      type: array
                                    exposeHeaders:
                                      description: The set of HTTP response headers
                                        that browsers are allowed to access. This
                                        configures the Access-Control-Expose-Headers
                                        header. The set of HTTP response headers that
                                        browsers are allowed to access. This configures
                                        the Access-Control-Expose-Headers header.
                                      items:
                                        type: string
                                      type: array
                                    maxAge:
                                      description: 'An optional duration specifying
                                        how long browsers can cache the results of
                                        a preflight request. This configures the Access-Control-Max-Age
                                        header. Example: 5h30m. An optional duration
                                        specifying how long browsers can cache the
                                        results of a preflight request. This configures
                                        the Access-Control-Max-Age header. Example:
                                        `5h30m`.'
                                      type: string
                                  type: object
                                type: array
                              dockerfilePath:
                                description: The path to a Dockerfile relative to
                                  the root of the repo. If set, overrides usage of
                                  buildpacks. The path to a Dockerfile relative to
                                  the root of the repo. If set, overrides usage of
                                  buildpacks.
                                type: string
                              env:
                                description: Describes an app-wide environment variable
                                  made available to all components.
                                items:
                                  properties:
                                    key:
                                      description: The name of the environment variable.
                                        The name of the environment variable.
                                      type: string
                                    scope:
                                      description: The visibility scope of the environment
                                        variable. One of RUN_TIME, BUILD_TIME, or
                                        RUN_AND_BUILD_TIME (default). The visibility
                                        scope of the environment variable.
                                      type: string
                                    type:
                                      description: 'The domain type, which can be
                                        one of the following: The type of the environment
                                        variable.'
                                      type: string
                                    valueSecretRef:
                                      description: The value of the environment variable.
                                        The value of the environment variable.
                                      properties:
                                        key:
                                          description: The key to select.
                                          type: string
                                        name:
                                          description: Name of the secret.
                                          type: string
                                        namespace:
                                          description: Namespace of the secret.
                                          type: string
                                      required:
                                      - key
                                      - name
                                      - namespace
                                      type: object
                                  type: object
                                type: array
                              environmentSlug:
                                description: An environment slug describing the type
                                  of this app. An environment slug describing the
                                  type of this app.
                                type: string
                              errorDocument:
                                description: The name of the error document to use
                                  when serving this static site. The name of the error
                                  document to use when serving this static site.
                                type: string
                              git:
                                description: A Git repo to use as the component's
                                  source. The repository must be able to be cloned
                                  without authentication.  Only one of git, github
                                  or gitlab  may be set
                                items:
                                  properties:
                                    branch:
                                      description: The name of the branch to use.
                                        The name of the branch to use.
                                      type: string
                                    repoCloneUrl:
                                      description: The clone URL of the repo. The
                                        clone URL of the repo.
                                      type: string
                                  type: object
                                type: array
                              github:
                                description: A GitHub repo to use as the component's
                                  source. DigitalOcean App Platform must have access
                                  to the repository. Only one of git, github, gitlab,
                                  or image may be set.
                                items:
                                  properties:
                                    branch:
                                      description: The name of the branch to use.
                                        The name of the branch to use.
                                      type: string
                                    deployOnPush:
                                      description: Whether to automatically deploy
                                        new commits made to the repo. Whether to automatically
                                        deploy new commits made to the repo
                                      type: boolean
                                    repo:
                                      description: The name of the repo in the format
                                        owner/repo. The name of the repo in the format
                                        `owner/repo`.
                                      type: string
                                  type: object
                                type: array
                              gitlab:
                                description: A Gitlab repo to use as the component's
                                  source. DigitalOcean App Platform must have access
                                  to the repository. Only one of git, github, gitlab,
                                  or image may be set.
                                items:
                                  properties:
                                    branch:
                                      description: The name of the branch to use.
                                        The name of the branch to use.
                                      type: string
                                    deployOnPush:
                                      description: Whether to automatically deploy
                                        new commits made to the repo. Whether to automatically
                                        deploy new commits made to the repo
                                      type: boolean
                                    repo:
                                      description: The name of the repo in the format
                                        owner/repo. The name of the repo in the format
                                        `owner/repo`.
                                      type: string
                                  type: object
                                type: array
                              indexDocument:
                                description: The name of the index document to use
                                  when serving this static site. The name of the index
                                  document to use when serving this static site.
                                type: string
                              name:
                                description: The name of the app. Must be unique across
                                  all apps in the same account. The name of the component
                                type: string
                              outputDir:
                                description: 'An optional path to where the built
                                  assets will be located, relative to the build context.
                                  If not set, App Platform will automatically scan
                                  for these directory names: _static, dist, public.
                                  An optional path to where the built assets will
                                  be located, relative to the build context. If not
                                  set, App Platform will automatically scan for these
                                  directory names: `_static`, `dist`, `public`.'
                                type: string
                              routes:
                                description: (Deprecated - use ingress) An HTTP paths
                                  that should be routed to this component.
                                items:
                                  properties:
                                    path:
                                      description: The path to match on. Path specifies
                                        an route by HTTP path prefix. Paths must start
                                        with / and must be unique within the app.
                                      type: string
                                    preservePathPrefix:
                                      description: An optional boolean flag to preserve
                                        the path that is forwarded to the backend
                                        service. By default, the HTTP request path
                                        will be trimmed from the left when forwarded
                                        to the component. An optional flag to preserve
                                        the path that is forwarded to the backend
                                        service.
                                      type: boolean
                                  type: object
                                type: array
                              sourceDir:
                                description: An optional path to the working directory
                                  to use for the build. An optional path to the working
                                  directory to use for the build.
                                type: string
                            type: object
                          type: array
                        worker:
                          items:
                            properties:
                              alert:
                                description: Describes an alert policy for the app.
                                  Alert policies for the app component
                                items:
                                  properties:
                                    disabled:
                                      description: 'Determines whether or not the
                                        alert is disabled (default: false).'
                                      type: boolean
                                    operator:
                                      description: The operator to use. This is either
                                        of GREATER_THAN or LESS_THAN.
                                      type: string
                                    rule:
                                      description: 'The type of the alert to configure.
                                        Top-level app alert policies can be: DEPLOYMENT_FAILED,
                                        DEPLOYMENT_LIVE, DOMAIN_FAILED, or DOMAIN_LIVE.'
                                      type: string
                                    value:
                                      description: The value of the environment variable.
                                      type: number
                                    window:
                                      description: 'The time before alerts should
                                        be triggered. This is may be one of: FIVE_MINUTES,
                                        TEN_MINUTES, THIRTY_MINUTES, ONE_HOUR.'
                                      type: string
                                  type: object
                                type: array
                              buildCommand:
                                description: An optional build command to run while
                                  building this component from source. An optional
                                  build command to run while building this component
                                  from source.
                                type: string
                              dockerfilePath:
                                description: The path to a Dockerfile relative to
                                  the root of the repo. If set, overrides usage of
                                  buildpacks. The path to a Dockerfile relative to
                                  the root of the repo. If set, overrides usage of
                                  buildpacks.
                                type: string
                              env:
                                description: Describes an app-wide environment variable
                                  made available to all components.
                                items:
                                  properties:
                                    key:
                                      description: The name of the environment variable.
                                        The name of the environment variable.
                                      type: string
                                    scope:
                                      description: The visibility scope of the environment
                                        variable. One of RUN_TIME, BUILD_TIME, or
                                        RUN_AND_BUILD_TIME (default). The visibility
                                        scope of the environment variable.
                                      type: string
                                    type:
                                      description: 'The domain type, which can be
                                        one of the following: The type of the environment
                                        variable.'
                                      type: string
                                    valueSecretRef:
                                      description: The value of the environment variable.
                                        The value of the environment variable.
                                      properties:
                                        key:
                                          description: The key to select.
                                          type: string
                                        name:
                                          description: Name of the secret.
                                          type: string
                                        namespace:
                                          description: Namespace of the secret.
                                          type: string
                                      required:
                                      - key
                                      - name
                                      - namespace
                                      type: object
                                  type: object
                                type: array
                              environmentSlug:
                                description: An environment slug describing the type
                                  of this app. An environment slug describing the
                                  type of this app.
                                type: string
                              git:
                                description: A Git repo to use as the component's
                                  source. The repository must be able to be cloned
                                  without authentication.  Only one of git, github
                                  or gitlab  may be set
                                items:
                                  properties:
                                    branch:
                                      description: The name of the branch to use.
                                        The name of the branch to use.
                                      type: string
                                    repoCloneUrl:
                                      description: The clone URL of the repo. The
                                        clone URL of the repo.
                                      type: string
                                  type: object
                                type: array
                              github:
                                description: A GitHub repo to use as the component's
                                  source. DigitalOcean App Platform must have access
                                  to the repository. Only one of git, github, gitlab,
                                  or image may be set.
                                items:
                                  properties:
                                    branch:
                                      description: The name of the branch to use.
                                        The name of the branch to use.
                                      type: string
                                    deployOnPush:
                                      description: Whether to automatically deploy
                                        new commits made to the repo. Whether to automatically
                                        deploy new commits made to the repo
                                      type: boolean
                                    repo:
                                      description: The name of the repo in the format
                                        owner/repo. The name of the repo in the format
                                        `owner/repo`.
                                      type: string
                                  type: object
                                type: array
                              gitlab:
                                description: A Gitlab repo to use as the component's
                                  source. DigitalOcean App Platform must have access
                                  to the repository. Only one of git, github, gitlab,
                                  or image may be set.
                                items:
                                  properties:
                                    branch:
                                      description: The name of the branch to use.
                                        The name of the branch to use.
                                      type: string
                                    deployOnPush:
                                      description: Whether to automatically deploy
                                        new commits made to the repo. Whether to automatically
                                        deploy new commits made to the repo
                                      type: boolean
                                    repo:
                                      description: The name of the repo in the format
                                        owner/repo. The name of the repo in the format
                                        `owner/repo`.
                                      type: string
                                  type: object
                                type: array
                              image:
                                description: An image to use as the component's source.
                                  Only one of git, github, gitlab, or image may be
                                  set.
                                items:
                                  properties:
                                    deployOnPush:
                                      description: Whether to automatically deploy
                                        new commits made to the repo. Configures automatically
                                        deploying images pushed to DOCR.
                                      items:
                                        properties:
                                          enabled:
                                            description: Whether to automatically
                                              deploy images pushed to DOCR. Whether
                                              to automatically deploy images pushed
                                              to DOCR.
                                            type: boolean
                                        type: object
                                      type: array
                                    registry:
                                      description: The registry name. Must be left
                                        empty for the DOCR registry type. Required
                                        for the DOCKER_HUB registry type. The registry
                                        name. Must be left empty for the DOCR registry
                                        type.
                                      type: string
                                    registryType:
                                      description: The registry type. One of DOCR
                                        (DigitalOcean container registry) or DOCKER_HUB.
                                        The registry type.
                                      type: string
                                    repository:
                                      description: The repository name. The repository
                                        name.
                                      type: string
                                    tag:
                                      description: The repository tag. Defaults to
                                        latest if not provided. The repository tag.
                                        Defaults to latest if not provided.
                                      type: string
                                  type: object
                                type: array
                              instanceCount:
                                description: The amount of instances that this component
                                  should be scaled to. The amount of instances that
                                  this component should be scaled to.
                                type: number
                              instanceSizeSlug:
                                description: 'The instance size to use for this component.
                                  This determines the plan (basic or professional)
                                  and the available CPU and memory. The list of available
                                  instance sizes can be found with the API or using
                                  the doctl CLI (doctl apps tier instance-size list).
                                  Default: basic-xxs The instance size to use for
                                  this component.'
                                type: string
                              logDestination:
                                description: Describes a log forwarding destination.
                                  Logs
                                items:
                                  properties:
                                    datadog:
                                      description: Datadog configuration. Datadog
                                        configuration.
                                      items:
                                        properties:
                                          apiKey:
                                            description: Datadog API key. Datadog
                                              API key.
                                            type: string
                                          endpoint:
                                            description: Papertrail syslog endpoint.
                                              Datadog HTTP log intake endpoint.
                                            type: string
                                        type: object
                                      type: array
                                    logtail:
                                      description: Logtail configuration. Logtail
                                        configuration.
                                      items:
                                        properties:
                                          token:
                                            description: Logtail token. Logtail token.
                                            type: string
                                        type: object
                                      type: array
                                    name:
                                      description: The name of the app. Must be unique
                                        across all apps in the same account. Name
                                        of the log destination
                                      type: string
                                    papertrail:
                                      description: Papertrail configuration. Papertrail
                                        configuration.
                                      items:
                                        properties:
                                          endpoint:
                                            description: Papertrail syslog endpoint.
                                              Papertrail syslog endpoint.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              name:
                                description: The name of the app. Must be unique across
                                  all apps in the same account. The name of the component
                                type: string
                              runCommand:
                                description: An optional run command to override the
                                  component's default. An optional run command to
                                  override the component's default.
                                type: string
                              sourceDir:
                                description: An optional path to the working directory
                                  to use for the build. An optional path to the working
                                  directory to use for the build.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                type: object
              initProvider:
                description: THIS IS A BETA FIELD. It will be honored unless the Management
                  Policies feature flag is disabled. InitProvider holds the same fields
                  as ForProvider, with the exception of Identifier and other resource
                  reference fields. The fields that are in InitProvider are merged
                  into ForProvider when the resource is created. The same fields are
                  also added to the terraform ignore_changes hook, to avoid updating
                  them after creation. This is useful for fields that are required
                  on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them,
                  like an autoscaler.
                properties:
                  spec:
                    description: A DigitalOcean App spec describing the app. A DigitalOcean
                      App Platform Spec
                    items:
                      properties:
                        alert:
                          description: Describes an alert policy for the app.
                          items:
                            properties:
                              disabled:
                                description: 'Determines whether or not the alert
                                  is disabled (default: false).'
                                type: boolean
                              rule:
                                description: 'The type of the alert to configure.
                                  Top-level app alert policies can be: DEPLOYMENT_FAILED,
                                  DEPLOYMENT_LIVE, DOMAIN_FAILED, or DOMAIN_LIVE.'
                                type: string
                            type: object
                          type: array
                        database:
                          items:
                            properties:
                              clusterName:
                                description: The name of the underlying DigitalOcean
                                  DBaaS cluster. This is required for production databases.
                                  For dev databases, if cluster_name is not set, a
                                  new cluster will be provisioned. The name of the
                                  underlying DigitalOcean DBaaS cluster. This is required
                                  for production databases. For dev databases, if
                                  cluster_name is not set, a new cluster will be provisioned.
                                type: string
                              dbName:
                                description: The name of the MySQL or PostgreSQL database
                                  to configure. The name of the MySQL or PostgreSQL
                                  database to configure.
                                type: string
                              dbUser:
                                description: The name of the MySQL or PostgreSQL user
                                  to configure. The name of the MySQL or PostgreSQL
                                  user to configure.
                                type: string
                              engine:
                                description: The database engine to use (MYSQL, PG,
                                  REDIS, or MONGODB). The database engine to use.
                                type: string
                              name:
                                description: The name of the app. Must be unique across
                                  all apps in the same account. The name of the component
                                type: string
                              production:
                                description: Whether this is a production or dev database.
                                  Whether this is a production or dev database.
                                type: boolean
                              version:
                                description: The version of the database engine. The
                                  version of the database engine.
                                type: string
                            type: object
                          type: array
                        domain:
                          description: Describes a domain where the application will
                            be made available.
                          items:
                            properties:
                              name:
                                description: The name of the app. Must be unique across
                                  all apps in the same account. The hostname for the
                                  domain.
                                type: string
                              type:
                                description: 'The domain type, which can be one of
                                  the following: The type of the domain.'
                                type: string
                              wildcard:
                                description: A boolean indicating whether the domain
                                  includes all sub-domains, in addition to the given
                                  domain. Indicates whether the domain includes all
                                  sub-domains, in addition to the given domain.
                                type: boolean
                              zone:
                                description: If the domain uses DigitalOcean DNS and
                                  you would like App Platform to automatically manage
                                  it for you, set this to the name of the domain on
                                  your account. If the domain uses DigitalOcean DNS
                                  and you would like App Platform to automatically
                                  manage it for you, set this to the name of the domain
                                  on your account.
                                type: string
                            type: object
                          type: array
                        domains:
                          items:
                            type: string
                          type: array
                        env:
                          description: Describes an app-wide environment variable
                            made available to all components.
                          items:
                            properties:
                              key:
                                description: The name of the environment variable.
                                  The name of the environment variable.
                                type: string
                              scope:
                                description: The visibility scope of the environment
                                  variable. One of RUN_TIME, BUILD_TIME, or RUN_AND_BUILD_TIME
                                  (default). The visibility scope of the environment
                                  variable.
                                type: string
                              type:
                                description: 'The domain type, which can be one of
                                  the following: The type of the environment variable.'
                                type: string
                            type: object
                          type: array
                        features:
                          description: A list of the features applied to the app.
                            The default buildpack can be overridden here. List of
                            available buildpacks can be found using the doctl CLI
                            List of features which is applied to the app
                          items:
                            type: string
                          type: array
                        function:
                          items:
                            properties:
                              alert:
                                description: Describes an alert policy for the app.
                                  Alert policies for the app component
                                items:
                                  properties:
                                    disabled:
                                      description: 'Determines whether or not the
                                        alert is disabled (default: false).'
                                      type: boolean
                                    operator:
                                      description: The operator to use. This is either
                                        of GREATER_THAN or LESS_THAN.
                                      type: string
                                    rule:
                                      description: 'The type of the alert to configure.
                                        Top-level app alert policies can be: DEPLOYMENT_FAILED,
                                        DEPLOYMENT_LIVE, DOMAIN_FAILED, or DOMAIN_LIVE.'
                                      type: string
                                    value:
                                      description: The value of the environment variable.
                                      type: number
                                    window:
                                      description: 'The time before alerts should
                                        be triggered. This is may be one of: FIVE_MINUTES,
                                        TEN_MINUTES, THIRTY_MINUTES, ONE_HOUR.'
                                      type: string
                                  type: object
                                type: array
                              cors:
                                description: The CORS policies of the app.
                                items:
                                  properties:
                                    allowCredentials:
                                      description: Whether browsers should expose
                                        the response to the client-side JavaScript
                                        code when the request's credentials mode is
                                        include. This configures the Access-Control-Allow-Credentials
                                        header. Whether browsers should expose the
                                        response to the client-side JavaScript code
                                        when the request’s credentials mode is `include`.
                                        This configures the Access-Control-Allow-Credentials
                                        header.
                                      type: boolean
                                    allowHeaders:
                                      description: The set of allowed HTTP request
                                        headers. This configures the Access-Control-Allow-Headers
                                        header. The set of allowed HTTP request headers.
                                        This configures the Access-Control-Allow-Headers
                                        header.
                                      items:
                                        type: string
                                      type: array
                                    allowMethods:
                                      description: The set of allowed HTTP methods.
                                        This configures the Access-Control-Allow-Methods
                                        header. The set of allowed HTTP methods. This
                                        configures the Access-Control-Allow-Methods
                                        header.
                                      items:
                                        type: string
                                      type: array
                                    allowOrigins:
                                      description: The Access-Control-Allow-Origin
                                        can be The set of allowed CORS origins. This
                                        configures the Access-Control-Allow-Origin
                                        header.
                                      items:
                                        properties:
                                          exact:
                                            description: The Access-Control-Allow-Origin
                                              header will be set to the client's origin
                                              only if the client's origin exactly
                                              matches the value you provide. Exact
                                              string match.
                                            type: string
                                          prefix:
                                            description: Prefix-based match. Prefix-based
                                              match.
                                            type: string
                                          regex:
                                            description: The Access-Control-Allow-Origin
                                              header will be set to the client's origin
                                              if the client’s origin matches the regex
                                              you provide, in RE2 style syntax. RE2
                                              style regex-based match.
                                            type: string
                                        type: object
                                      type: array
                                    exposeHeaders:
                                      description: The set of HTTP response headers
                                        that browsers are allowed to access. This
                                        configures the Access-Control-Expose-Headers
                                        header. The set of HTTP response headers that
                                        browsers are allowed to access. This configures
                                        the Access-Control-Expose-Headers header.
                                      items:
                                        type: string
                                      type: array
                                    maxAge:
                                      description: 'An optional duration specifying
                                        how long browsers can cache the results of
                                        a preflight request. This configures the Access-Control-Max-Age
                                        header. Example: 5h30m. An optional duration
                                        specifying how long browsers can cache the
                                        results of a preflight request. This configures
                                        the Access-Control-Max-Age header. Example:
                                        `5h30m`.'
                                      type: string
                                  type: object
                                type: array
                              env:
                                description: Describes an app-wide environment variable
                                  made available to all components.
                                items:
                                  properties:
                                    key:
                                      description: The name of the environment variable.
                                        The name of the environment variable.
                                      type: string
                                    scope:
                                      description: The visibility scope of the environment
                                        variable. One of RUN_TIME, BUILD_TIME, or
                                        RUN_AND_BUILD_TIME (default). The visibility
                                        scope of the environment variable.
                                      type: string
                                    type:
                                      description: 'The domain type, which can be
                                        one of the following: The type of the environment
                                        variable.'
                                      type: string
                                  type: object
                                type: array
                              git:
                                description: A Git repo to use as the component's
                                  source. The repository must be able to be cloned
                                  without authentication.  Only one of git, github
                                  or gitlab  may be set
                                items:
                                  properties:
                                    branch:
                                      description: The name of the branch to use.
                                        The name of the branch to use.
                                      type: string
                                    repoCloneUrl:
                                      description: The clone URL of the repo. The
                                        clone URL of the repo.
                                      type: string
                                  type: object
                                type: array
                              github:
                                description: A GitHub repo to use as the component's
                                  source. DigitalOcean App Platform must have access
                                  to the repository. Only one of git, github, gitlab,
                                  or image may be set.
                                items:
                                  properties:
                                    branch:
                                      description: The name of the branch to use.
                                        The name of the branch to use.
                                      type: string
                                    deployOnPush:
                                      description: Whether to automatically deploy
                                        new commits made to the repo. Whether to automatically
                                        deploy new commits made to the repo
                                      type: boolean
                                    repo:
                                      description: The name of the repo in the format
                                        owner/repo. The name of the repo in the format
                                        `owner/repo`.
                                      type: string
                                  type: object
                                type: array
                              gitlab:
                                description: A Gitlab repo to use as the component's
                                  source. DigitalOcean App Platform must have access
                                  to the repository. Only one of git, github, gitlab,
                                  or image may be set.
                                items:
                                  properties:
                                    branch:
                                      description: The name of the branch to use.
                                        The name of the branch to use.
                                      type: string
                                    deployOnPush:
                                      description: Whether to automatically deploy
                                        new commits made to the repo. Whether to automatically
                                        deploy new commits made to the repo
                                      type: boolean
                                    repo:
                                      description: The name of the repo in the format
                                        owner/repo. The name of the repo in the format
                                        `owner/repo`.
                                      type: string
                                  type: object
                                type: array
                              logDestination:
                                description: Describes a log forwarding destination.
                                  Logs
                                items:
                                  properties:
                                    datadog:
                                      description: Datadog configuration. Datadog
                                        configuration.
                                      items:
                                        properties:
                                          apiKey:
                                            description: Datadog API key. Datadog
                                              API key.
                                            type: string
                                          endpoint:
                                            description: Papertrail syslog endpoint.
                                              Datadog HTTP log intake endpoint.
                                            type: string
                                        type: object
                                      type: array
                                    logtail:
                                      description: Logtail configuration. Logtail
                                        configuration.
                                      items:
                                        properties:
                                          token:
                                            description: Logtail token. Logtail token.
                                            type: string
                                        type: object
                                      type: array
                                    name:
                                      description: The name of the app. Must be unique
                                        across all apps in the same account. Name
                                        of the log destination
                                      type: string
                                    papertrail:
                                      description: Papertrail configuration. Papertrail
                                        configuration.
                                      items:
                                        properties:
                                          endpoint:
                                            description: Papertrail syslog endpoint.
                                              Papertrail syslog endpoint.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              name:
                                description: The name of the app. Must be unique across
                                  all apps in the same account. The name of the component
                                type: string
                              routes:
                                description: (Deprecated - use ingress) An HTTP paths
                                  that should be routed to this component.
                                items:
                                  properties:
                                    path:
                                      description: The path to match on. Path specifies
                                        an route by HTTP path prefix. Paths must start
                                        with / and must be unique within the app.
                                      type: string
                                    preservePathPrefix:
                                      description: An optional boolean flag to preserve
                                        the path that is forwarded to the backend
                                        service. By default, the HTTP request path
                                        will be trimmed from the left when forwarded
                                        to the component. An optional flag to preserve
                                        the path that is forwarded to the backend
                                        service.
                                      type: boolean
                                  type: object
                                type: array
                              sourceDir:
                                description: An optional path to the working directory
                                  to use for the build. An optional path to the working
                                  directory to use for the build.
                                type: string
                            type: object
                          type: array
                        ingress:
                          description: Specification for component routing, rewrites,
                            and redirects.
                          items:
                            properties:
                              rule:
                                description: 'The type of the alert to configure.
                                  Top-level app alert policies can be: DEPLOYMENT_FAILED,
                                  DEPLOYMENT_LIVE, DOMAIN_FAILED, or DOMAIN_LIVE.'
                                items:
                                  properties:
                                    component:
                                      description: The component to route to. Only
                                        one of component or redirect may be set.
                                      items:
                                        properties:
                                          name:
                                            description: The name of the app. Must
                                              be unique across all apps in the same
                                              account.
                                            type: string
                                          preservePathPrefix:
                                            description: An optional boolean flag
                                              to preserve the path that is forwarded
                                              to the backend service. By default,
                                              the HTTP request path will be trimmed
                                              from the left when forwarded to the
                                              component.
                                            type: boolean
                                          rewrite:
                                            description: An optional field that will
                                              rewrite the path of the component to
                                              be what is specified here. This is mutually
                                              exclusive with preserve_path_prefix.
                                            type: string
                                        type: object
                                      type: array
                                    cors:
                                      description: The CORS policies of the app.
                                      items:
                                        properties:
                                          allowCredentials:
                                            description: Whether browsers should expose
                                              the response to the client-side JavaScript
                                              code when the request's credentials
                                              mode is include. This configures the
                                              Access-Control-Allow-Credentials header.
                                              Whether browsers should expose the response
                                              to the client-side JavaScript code when
                                              the request’s credentials mode is `include`.
                                              This configures the Access-Control-Allow-Credentials
                                              header.
                                            type: boolean
                                          allowHeaders:
                                            description: The set of allowed HTTP request
                                              headers. This configures the Access-Control-Allow-Headers
                                              header. The set of allowed HTTP request
                                              headers. This configures the Access-Control-Allow-Headers
                                              header.
                                            items:
                                              type: string
                                            type: array
                                          allowMethods:
                                            description: The set of allowed HTTP methods.
                                              This configures the Access-Control-Allow-Methods
                                              header. The set of allowed HTTP methods.
                                              This configures the Access-Control-Allow-Methods
                                              header.
                                            items:
                                              type: string
                                            type: array
                                          allowOrigins:
                                            description: The Access-Control-Allow-Origin
                                              can be The set of allowed CORS origins.
                                              This configures the Access-Control-Allow-Origin
                                              header.
                                            items:
                                              properties:
                                                exact:
                                                  description: The Access-Control-Allow-Origin
                                                    header will be set to the client's
                                                    origin only if the client's origin
                                                    exactly matches the value you
                                                    provide. Exact string match.
                                                  type: string
                                                prefix:
                                                  description: Prefix-based match.
                                                    Prefix-based match.
                                                  type: string
                                                regex:
                                                  description: The Access-Control-Allow-Origin
                                                    header will be set to the client's
                                                    origin if the client’s origin
                                                    matches the regex you provide,
                                                    in RE2 style syntax. RE2 style
                                                    regex-based match.
                                                  type: string
                                              type: object
                                            type: array
                                          exposeHeaders:
                                            description: The set of HTTP response
                                              headers that browsers are allowed to
                                              access. This configures the Access-Control-Expose-Headers
                                              header. The set of HTTP response headers
                                              that browsers are allowed to access.
                                              This configures the Access-Control-Expose-Headers
                                              header.
                                            items:
                                              type: string
                                            type: array
                                          maxAge:
                                            description: 'An optional duration specifying
                                              how long browsers can cache the results
                                              of a preflight request. This configures
                                              the Access-Control-Max-Age header. Example:
                                              5h30m. An optional duration specifying
                                              how long browsers can cache the results
                                              of a preflight request. This configures
                                              the Access-Control-Max-Age header. Example:
                                              `5h30m`.'
                                            type: string
                                        type: object
                                      type: array
                                    match:
                                      description: The match configuration for the
                                        rule
                                      items:
                                        properties:
                                          path:
                                            description: The path to match on.
                                            items:
                                              properties:
                                                prefix:
                                                  description: Prefix-based match.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    redirect:
                                      description: The redirect configuration for
                                        the rule. Only one of component or redirect
                                        may be set.
                                      items:
                                        properties:
                                          authority:
                                            description: The authority/host to redirect
                                              to. This can be a hostname or IP address.
                                            type: string
                                          port:
                                            description: The port to redirect to.
                                            type: number
                                          redirectCode:
                                            description: The redirect code to use.
                                              Supported values are 300, 301, 302,
                                              303, 304, 307, 308.
                                            type: number
                                          scheme:
                                            description: The scheme to redirect to.
                                              Supported values are http or https
                                            type: string
                                          uri:
                                            description: An optional URI path to redirect
                                              to.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                            type: object
                          type: array
                        job:
                          items:
                            properties:
                              alert:
                                description: Describes an alert policy for the app.
                                  Alert policies for the app component
                                items:
                                  properties:
                                    disabled:
                                      description: 'Determines whether or not the
                                        alert is disabled (default: false).'
                                      type: boolean
                                    operator:
                                      description: The operator to use. This is either
                                        of GREATER_THAN or LESS_THAN.
                                      type: string
                                    rule:
                                      description: 'The type of the alert to configure.
                                        Top-level app alert policies can be: DEPLOYMENT_FAILED,
                                        DEPLOYMENT_LIVE, DOMAIN_FAILED, or DOMAIN_LIVE.'
                                      type: string
                                    value:
                                      description: The value of the environment variable.
                                      type: number
                                    window:
                                      description: 'The time before alerts should
                                        be triggered. This is may be one of: FIVE_MINUTES,
                                        TEN_MINUTES, THIRTY_MINUTES, ONE_HOUR.'
                                      type: string
                                  type: object
                                type: array
                              buildCommand:
                                description: An optional build command to run while
                                  building this component from source. An optional
                                  build command to run while building this component
                                  from source.
                                type: string
                              dockerfilePath:
                                description: The path to a Dockerfile relative to
                                  the root of the repo. If set, overrides usage of
                                  buildpacks. The path to a Dockerfile relative to
                                  the root of the repo. If set, overrides usage of
                                  buildpacks.
                                type: string
                              env:
                                description: Describes an app-wide environment variable
                                  made available to all components.
                                items:
                                  properties:
                                    key:
                                      description: The name of the environment variable.
                                        The name of the environment variable.
                                      type: string
                                    scope:
                                      description: The visibility scope of the environment
                                        variable. One of RUN_TIME, BUILD_TIME, or
                                        RUN_AND_BUILD_TIME (default). The visibility
                                        scope of the environment variable.
                                      type: string
                                    type:
                                      description: 'The domain type, which can be
                                        one of the following: The type of the environment
                                        variable.'
                                      type: string
                                  type: object
                                type: array
                              environmentSlug:
                                description: An environment slug describing the type
                                  of this app. An environment slug describing the
                                  type of this app.
                                type: string
                              git:
                                description: A Git repo to use as the component's
                                  source. The repository must be able to be cloned
                                  without authentication.  Only one of git, github
                                  or gitlab  may be set
                                items:
                                  properties:
                                    branch:
                                      description: The name of the branch to use.
                                        The name of the branch to use.
                                      type: string
                                    repoCloneUrl:
                                      description: The clone URL of the repo. The
                                        clone URL of the repo.
                                      type: string
                                  type: object
                                type: array
                              github:
                                description: A GitHub repo to use as the component's
                                  source. DigitalOcean App Platform must have access
                                  to the repository. Only one of git, github, gitlab,
                                  or image may be set.
                                items:
                                  properties:
                                    branch:
                                      description: The name of the branch to use.
                                        The name of the branch to use.
                                      type: string
                                    deployOnPush:
                                      description: Whether to automatically deploy
                                        new commits made to the repo. Whether to automatically
                                        deploy new commits made to the repo
                                      type: boolean
                                    repo:
                                      description: The name of the repo in the format
                                        owner/repo. The name of the repo in the format
                                        `owner/repo`.
                                      type: string
                                  type: object
                                type: array
                              gitlab:
                                description: A Gitlab repo to use as the component's
                                  source. DigitalOcean App Platform must have access
                                  to the repository. Only one of git, github, gitlab,
                                  or image may be set.
                                items:
                                  properties:
                                    branch:
                                      description: The name of the branch to use.
                                        The name of the branch to use.
                                      type: string
                                    deployOnPush:
                                      description: Whether to automatically deploy
                                        new commits made to the repo. Whether to automatically
                                        deploy new commits made to the repo
                                      type: boolean
                                    repo:
                                      description: The name of the repo in the format
                                        owner/repo. The name of the repo in the format
                                        `owner/repo`.
                                      type: string
                                  type: object
                                type: array
                              image:
                                description: An image to use as the component's source.
                                  Only one of git, github, gitlab, or image may be
                                  set.
                                items:
                                  properties:
                                    deployOnPush:
                                      description: Whether to automatically deploy
                                        new commits made to the repo. Configures automatically
                                        deploying images pushed to DOCR.
                                      items:
                                        properties:
                                          enabled:
                                            description: Whether to automatically
                                              deploy images pushed to DOCR. Whether
                                              to automatically deploy images pushed
                                              to DOCR.
                                            type: boolean
                                        type: object
                                      type: array
                                    registry:
                                      description: The registry name. Must be left
                                        empty for the DOCR registry type. Required
                                        for the DOCKER_HUB registry type. The registry
                                        name. Must be left empty for the DOCR registry
                                        type.
                                      type: string
                                    registryType:
                                      description: The registry type. One of DOCR
                                        (DigitalOcean container registry) or DOCKER_HUB.
                                        The registry type.
                                      type: string
                                    repository:
                                      description: The repository name. The repository
                                        name.
                                      type: string
                                    tag:
                                      description: The repository tag. Defaults to
                                        latest if not provided. The repository tag.
                                        Defaults to latest if not provided.
                                      type: string
                                  type: object
                                type: array
                              instanceCount:
                                description: The amount of instances that this component
                                  should be scaled to. The amount of instances that
                                  this component should be scaled to.
                                type: number
                              instanceSizeSlug:
                                description: 'The instance size to use for this component.
                                  This determines the plan (basic or professional)
                                  and the available CPU and memory. The list of available
                                  instance sizes can be found with the API or using
                                  the doctl CLI (doctl apps tier instance-size list).
                                  Default: basic-xxs The instance size to use for
                                  this component.'
                                type: string
                              kind:
                                description: 'The type of job and when it will be
                                  run during the deployment process. It may be one
                                  of: The type of job and when it will be run during
                                  the deployment process.'
                                type: string
                              logDestination:
                                description: Describes a log forwarding destination.
                                  Logs
                                items:
                                  properties:
                                    datadog:
                                      description: Datadog configuration. Datadog
                                        configuration.
                                      items:
                                        properties:
                                          apiKey:
                                            description: Datadog API key. Datadog
                                              API key.
                                            type: string
                                          endpoint:
                                            description: Papertrail syslog endpoint.
                                              Datadog HTTP log intake endpoint.
                                            type: string
                                        type: object
                                      type: array
                                    logtail:
                                      description: Logtail configuration. Logtail
                                        configuration.
                                      items:
                                        properties:
                                          token:
                                            description: Logtail token. Logtail token.
                                            type: string
                                        type: object
                                      type: array
                                    name:
                                      description: The name of the app. Must be unique
                                        across all apps in the same account. Name
                                        of the log destination
                                      type: string
                                    papertrail:
                                      description: Papertrail configuration. Papertrail
                                        configuration.
                                      items:
                                        properties:
                                          endpoint:
                                            description: Papertrail syslog endpoint.
                                              Papertrail syslog endpoint.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              name:
                                description: The name of the app. Must be unique across
                                  all apps in the same account. The name of the component
                                type: string
                              runCommand:
                                description: An optional run command to override the
                                  component's default. An optional run command to
                                  override the component's default.
                                type: string
                              sourceDir:
                                description: An optional path to the working directory
                                  to use for the build. An optional path to the working
                                  directory to use for the build.
                                type: string
                            type: object
                          type: array
                        name:
                          description: The name of the app. Must be unique across
                            all apps in the same account. The name of the app. Must
                            be unique across all apps in the same account.
                          type: string
                        region:
                          description: The slug for the DigitalOcean data center region
                            hosting the app. The slug for the DigitalOcean data center
                            region hosting the app
                          type: string
                        service:
                          items:
                            properties:
                              alert:
                                description: Describes an alert policy for the app.
                                  Alert policies for the app component
                                items:
                                  properties:
                                    disabled:
                                      description: 'Determines whether or not the
                                        alert is disabled (default: false).'
                                      type: boolean
                                    operator:
                                      description: The operator to use. This is either
                                        of GREATER_THAN or LESS_THAN.
                                      type: string
                                    rule:
                                      description: 'The type of the alert to configure.
                                        Top-level app alert policies can be: DEPLOYMENT_FAILED,
                                        DEPLOYMENT_LIVE, DOMAIN_FAILED, or DOMAIN_LIVE.'
                                      type: string
                                    value:
                                      description: The value of the environment variable.
                                      type: number
                                    window:
                                      description: 'The time before alerts should
                                        be triggered. This is may be one of: FIVE_MINUTES,
                                        TEN_MINUTES, THIRTY_MINUTES, ONE_HOUR.'
                                      type: string
                                  type: object
                                type: array
                              buildCommand:
                                description: An optional build command to run while
                                  building this component from source. An optional
                                  build command to run while building this component
                                  from source.
                                type: string
                              cors:
                                description: The CORS policies of the app.
                                items:
                                  properties:
                                    allowCredentials:
                                      description: Whether browsers should expose
                                        the response to the client-side JavaScript
                                        code when the request's credentials mode is
                                        include. This configures the Access-Control-Allow-Credentials
                                        header. Whether browsers should expose the
                                        response to the client-side JavaScript code
                                        when the request’s credentials mode is `include`.
                                        This configures the Access-Control-Allow-Credentials
                                        header.
                                      type: boolean
                                    allowHeaders:
                                      description: The set of allowed HTTP request
                                        headers. This configures the Access-Control-Allow-Headers
                                        header. The set of allowed HTTP request headers.
                                        This configures the Access-Control-Allow-Headers
                                        header.
                                      items:
                                        type: string
                                      type: array
                                    allowMethods:
                                      description: The set of allowed HTTP methods.
                                        This configures the Access-Control-Allow-Methods
                                        header. The set of allowed HTTP methods. This
                                        configures the Access-Control-Allow-Methods
                                        header.
                                      items:
                                        type: string
                                      type: array
                                    allowOrigins:
                                      description: The Access-Control-Allow-Origin
                                        can be The set of allowed CORS origins. This
                                        configures the Access-Control-Allow-Origin
                                        header.
                                      items:
                                        properties:
                                          exact:
                                            description: The Access-Control-Allow-Origin
                                              header will be set to the client's origin
                                              only if the client's origin exactly
                                              matches the value you provide. Exact
                                              string match.
                                            type: string
                                          prefix:
                                            description: Prefix-based match. Prefix-based
                                              match.
                                            type: string
                                          regex:
                                            description: The Access-Control-Allow-Origin
                                              header will be set to the client's origin
                                              if the client’s origin matches the regex
                                              you provide, in RE2 style syntax. RE2
                                              style regex-based match.
                                            type: string
                                        type: object
                                      type: array
                                    exposeHeaders:
                                      description: The set of HTTP response headers
                                        that browsers are allowed to access. This
                                        configures the Access-Control-Expose-Headers
                                        header. The set of HTTP response headers that
                                        browsers are allowed to access. This configures
                                        the Access-Control-Expose-Headers header.
                                      items:
                                        type: string
                                      type: array
                                    maxAge:
                                      description: 'An optional duration specifying
                                        how long browsers can cache the results of
                                        a preflight request. This configures the Access-Control-Max-Age
                                        header. Example: 5h30m. An optional duration
                                        specifying how long browsers can cache the
                                        results of a preflight request. This configures
                                        the Access-Control-Max-Age header. Example:
                                        `5h30m`.'
                                      type: string
                                  type: object
                                type: array
                              dockerfilePath:
                                description: The path to a Dockerfile relative to
                                  the root of the repo. If set, overrides usage of
                                  buildpacks. The path to a Dockerfile relative to
                                  the root of the repo. If set, overrides usage of
                                  buildpacks.
                                type: string
                              env:
                                description: Describes an app-wide environment variable
                                  made available to all components.
                                items:
                                  properties:
                                    key:
                                      description: The name of the environment variable.
                                        The name of the environment variable.
                                      type: string
                                    scope:
                                      description: The visibility scope of the environment
                                        variable. One of RUN_TIME, BUILD_TIME, or
                                        RUN_AND_BUILD_TIME (default). The visibility
                                        scope of the environment variable.
                                      type: string
                                    type:
                                      description: 'The domain type, which can be
                                        one of the following: The type of the environment
                                        variable.'
                                      type: string
                                  type: object
                                type: array
                              environmentSlug:
                                description: An environment slug describing the type
                                  of this app. An environment slug describing the
                                  type of this app.
                                type: string
                              git:
                                description: A Git repo to use as the component's
                                  source. The repository must be able to be cloned
                                  without authentication.  Only one of git, github
                                  or gitlab  may be set
                                items:
                                  properties:
                                    branch:
                                      description: The name of the branch to use.
                                        The name of the branch to use.
                                      type: string
                                    repoCloneUrl:
                                      description: The clone URL of the repo. The
                                        clone URL of the repo.
                                      type: string
                                  type: object
                                type: array
                              github:
                                description: A GitHub repo to use as the component's
                                  source. DigitalOcean App Platform must have access
                                  to the repository. Only one of git, github, gitlab,
                                  or image may be set.
                                items:
                                  properties:
                                    branch:
                                      description: The name of the branch to use.
                                        The name of the branch to use.
                                      type: string
                                    deployOnPush:
                                      description: Whether to automatically deploy
                                        new commits made to the repo. Whether to automatically
                                        deploy new commits made to the repo
                                      type: boolean
                                    repo:
                                      description: The name of the repo in the format
                                        owner/repo. The name of the repo in the format
                                        `owner/repo`.
                                      type: string
                                  type: object
                                type: array
                              gitlab:
                                description: A Gitlab repo to use as the component's
                                  source. DigitalOcean App Platform must have access
                                  to the repository. Only one of git, github, gitlab,
                                  or image may be set.
                                items:
                                  properties:
                                    branch:
                                      description: The name of the branch to use.
                                        The name of the branch to use.
                                      type: string
                                    deployOnPush:
                                      description: Whether to automatically deploy
                                        new commits made to the repo. Whether to automatically
                                        deploy new commits made to the repo
                                      type: boolean
                                    repo:
                                      description: The name of the repo in the format
                                        owner/repo. The name of the repo in the format
                                        `owner/repo`.
                                      type: string
                                  type: object
                                type: array
                              healthCheck:
                                description: A health check to determine the availability
                                  of this component.
                                items:
                                  properties:
                                    failureThreshold:
                                      description: The number of failed health checks
                                        before considered unhealthy. The number of
                                        failed health checks before considered unhealthy.
                                      type: number
                                    httpPath:
                                      description: The route path used for the HTTP
                                        health check ping. The route path used for
                                        the HTTP health check ping.
                                      type: string
                                    initialDelaySeconds:
                                      description: The number of seconds to wait before
                                        beginning health checks. The number of seconds
                                        to wait before beginning health checks.
                                      type: number
                                    periodSeconds:
                                      description: The number of seconds to wait between
                                        health checks. The number of seconds to wait
                                        between health checks.
                                      type: number
                                    port:
                                      description: The port to redirect to. The port
                                        on which the health check will be performed.
                                        If not set, the health check will be performed
                                        on the component's http_port.
                                      type: number
                                    successThreshold:
                                      description: The number of successful health
                                        checks before considered healthy. The number
                                        of successful health checks before considered
                                        healthy.
                                      type: number
                                    timeoutSeconds:
                                      description: The number of seconds after which
                                        the check times out. The number of seconds
                                        after which the check times out.
                                      type: number
                                  type: object
                                type: array
                              httpPort:
                                description: The internal port on which this service's
                                  run command will listen. The internal port on which
                                  this service's run command will listen.
                                type: number
                              image:
                                description: An image to use as the component's source.
                                  Only one of git, github, gitlab, or image may be
                                  set.
                                items:
                                  properties:
                                    deployOnPush:
                                      description: Whether to automatically deploy
                                        new commits made to the repo. Configures automatically
                                        deploying images pushed to DOCR.
                                      items:
                                        properties:
                                          enabled:
                                            description: Whether to automatically
                                              deploy images pushed to DOCR. Whether
                                              to automatically deploy images pushed
                                              to DOCR.
                                            type: boolean
                                        type: object
                                      type: array
                                    registry:
                                      description: The registry name. Must be left
                                        empty for the DOCR registry type. Required
                                        for the DOCKER_HUB registry type. The registry
                                        name. Must be left empty for the DOCR registry
                                        type.
                                      type: string
                                    registryType:
                                      description: The registry type. One of DOCR
                                        (DigitalOcean container registry) or DOCKER_HUB.
                                        The registry type.
                                      type: string
                                    repository:
                                      description: The repository name. The repository
                                        name.
                                      type: string
                                    tag:
                                      description: The repository tag. Defaults to
                                        latest if not provided. The repository tag.
                                        Defaults to latest if not provided.
                                      type: string
                                  type: object
                                type: array
                              instanceCount:
                                description: The amount of instances that this component
                                  should be scaled to. The amount of instances that
                                  this component should be scaled to.
                                type: number
                              instanceSizeSlug:
                                description: 'The instance size to use for this component.
                                  This determines the plan (basic or professional)
                                  and the available CPU and memory. The list of available
                                  instance sizes can be found with the API or using
                                  the doctl CLI (doctl apps tier instance-size list).
                                  Default: basic-xxs The instance size to use for
                                  this component.'
                                type: string
                              internalPorts:
                                description: A list of ports on which this service
                                  will listen for internal traffic.
                                items:
                                  type: number
                                type: array
                              logDestination:
                                description: Describes a log forwarding destination.
                                  Logs
                                items:
                                  properties:
                                    datadog:
                                      description: Datadog configuration. Datadog
                                        configuration.
                                      items:
                                        properties:
                                          apiKey:
                                            description: Datadog API key. Datadog
                                              API key.
                                            type: string
                                          endpoint:
                                            description: Papertrail syslog endpoint.
                                              Datadog HTTP log intake endpoint.
                                            type: string
                                        type: object
                                      type: array
                                    logtail:
                                      description: Logtail configuration. Logtail
                                        configuration.
                                      items:
                                        properties:
                                          token:
                                            description: Logtail token. Logtail token.
                                            type: string
                                        type: object
                                      type: array
                                    name:
                                      description: The name of the app. Must be unique
                                        across all apps in the same account. Name
                                        of the log destination
                                      type: string
                                    papertrail:
                                      description: Papertrail configuration. Papertrail
                                        configuration.
                                      items:
                                        properties:
                                          endpoint:
                                            description: Papertrail syslog endpoint.
                                              Papertrail syslog endpoint.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              name:
                                description: The name of the app. Must be unique across
                                  all apps in the same account. The name of the component
                                type: string
                              routes:
                                description: (Deprecated - use ingress) An HTTP paths
                                  that should be routed to this component.
                                items:
                                  properties:
                                    path:
                                      description: The path to match on. Path specifies
                                        an route by HTTP path prefix. Paths must start
                                        with / and must be unique within the app.
                                      type: string
                                    preservePathPrefix:
                                      description: An optional boolean flag to preserve
                                        the path that is forwarded to the backend
                                        service. By default, the HTTP request path
                                        will be trimmed from the left when forwarded
                                        to the component. An optional flag to preserve
                                        the path that is forwarded to the backend
                                        service.
                                      type: boolean
                                  type: object
                                type: array
                              runCommand:
                                description: An optional run command to override the
                                  component's default. An optional run command to
                                  override the component's default.
                                type: string
                              sourceDir:
                                description: An optional path to the working directory
                                  to use for the build. An optional path to the working
                                  directory to use for the build.
                                type: string
                            type: object
                          type: array
                        staticSite:
                          items:
                            properties:
                              buildCommand:
                                description: An optional build command to run while
                                  building this component from source. An optional
                                  build command to run while building this component
                                  from source.
                                type: string
                              catchallDocument:
                                description: The name of the document to use as the
                                  fallback for any requests to documents that are
                                  not found when serving this static site. The name
                                  of the document to use as the fallback for any requests
                                  to documents that are not found when serving this
                                  static site.
                                type: string
                              cors:
                                description: The CORS policies of the app.
                                items:
                                  properties:
                                    allowCredentials:
                                      description: Whether browsers should expose
                                        the response to the client-side JavaScript
                                        code when the request's credentials mode is
                                        include. This configures the Access-Control-Allow-Credentials
                                        header. Whether browsers should expose the
                                        response to the client-side JavaScript code
                                        when the request’s credentials mode is `include`.
                                        This configures the Access-Control-Allow-Credentials
                                        header.
                                      type: boolean
                                    allowHeaders:
                                      description: The set of allowed HTTP request
                                        headers. This configures the Access-Control-Allow-Headers
                                        header. The set of allowed HTTP request headers.
                                        This configures the Access-Control-Allow-Headers
                                        header.
                                      items:
                                        type: string
                                      type: array
                                    allowMethods:
                                      description: The set of allowed HTTP methods.
                                        This configures the Access-Control-Allow-Methods
                                        header. The set of allowed HTTP methods. This
                                        configures the Access-Control-Allow-Methods
                                        header.
                                      items:
                                        type: string
                                      type: array
                                    allowOrigins:
                                      description: The Access-Control-Allow-Origin
                                        can be The set of allowed CORS origins. This
                                        configures the Access-Control-Allow-Origin
                                        header.
                                      items:
                                        properties:
                                          exact:
                                            description: The Access-Control-Allow-Origin
                                              header will be set to the client's origin
                                              only if the client's origin exactly
                                              matches the value you provide. Exact
                                              string match.
                                            type: string
                                          prefix:
                                            description: Prefix-based match. Prefix-based
                                              match.
                                            type: string
                                          regex:
                                            description: The Access-Control-Allow-Origin
                                              header will be set to the client's origin
                                              if the client’s origin matches the regex
                                              you provide, in RE2 style syntax. RE2
                                              style regex-based match.
                                            type: string
                                        type: object
                                      type: array
                                    exposeHeaders:
                                      description: The set of HTTP response headers
                                        that browsers are allowed to access. This
                                        configures the Access-Control-Expose-Headers
                                        header. The set of HTTP response headers that
                                        browsers are allowed to access. This configures
                                        the Access-Control-Expose-Headers header.
                                      items:
                                        type: string
                                      type: array
                                    maxAge:
                                      description: 'An optional duration specifying
                                        how long browsers can cache the results of
                                        a preflight request. This configures the Access-Control-Max-Age
                                        header. Example: 5h30m. An optional duration
                                        specifying how long browsers can cache the
                                        results of a preflight request. This configures
                                        the Access-Control-Max-Age header. Example:
                                        `5h30m`.'
                                      type: string
                                  type: object
                                type: array
                              dockerfilePath:
                                description: The path to a Dockerfile relative to
                                  the root of the repo. If set, overrides usage of
                                  buildpacks. The path to a Dockerfile relative to
                                  the root of the repo. If set, overrides usage of
                                  buildpacks.
                                type: string
                              env:
                                description: Describes an app-wide environment variable
                                  made available to all components.
                                items:
                                  properties:
                                    key:
                                      description: The name of the environment variable.
                                        The name of the environment variable.
                                      type: string
                                    scope:
                                      description: The visibility scope of the environment
                                        variable. One of RUN_TIME, BUILD_TIME, or
                                        RUN_AND_BUILD_TIME (default). The visibility
                                        scope of the environment variable.
                                      type: string
                                    type:
                                      description: 'The domain type, which can be
                                        one of the following: The type of the environment
                                        variable.'
                                      type: string
                                  type: object
                                type: array
                              environmentSlug:
                                description: An environment slug describing the type
                                  of this app. An environment slug describing the
                                  type of this app.
                                type: string
                              errorDocument:
                                description: The name of the error document to use
                                  when serving this static site. The name of the error
                                  document to use when serving this static site.
                                type: string
                              git:
                                description: A Git repo to use as the component's
                                  source. The repository must be able to be cloned
                                  without authentication.  Only one of git, github
                                  or gitlab  may be set
                                items:
                                  properties:
                                    branch:
                                      description: The name of the branch to use.
                                        The name of the branch to use.
                                      type: string
                                    repoCloneUrl:
                                      description: The clone URL of the repo. The
                                        clone URL of the repo.
                                      type: string
                                  type: object
                                type: array
                              github:
                                description: A GitHub repo to use as the component's
                                  source. DigitalOcean App Platform must have access
                                  to the repository. Only one of git, github, gitlab,
                                  or image may be set.
                                items:
                                  properties:
                                    branch:
                                      description: The name of the branch to use.
                                        The name of the branch to use.
                                      type: string
                                    deployOnPush:
                                      description: Whether to automatically deploy
                                        new commits made to the repo. Whether to automatically
                                        deploy new commits made to the repo
                                      type: boolean
                                    repo:
                                      description: The name of the repo in the format
                                        owner/repo. The name of the repo in the format
                                        `owner/repo`.
                                      type: string
                                  type: object
                                type: array
                              gitlab:
                                description: A Gitlab repo to use as the component's
                                  source. DigitalOcean App Platform must have access
                                  to the repository. Only one of git, github, gitlab,
                                  or image may be set.
                                items:
                                  properties:
                                    branch:
                                      description: The name of the branch to use.
                                        The name of the branch to use.
                                      type: string
                                    deployOnPush:
                                      description: Whether to automatically deploy
                                        new commits made to the repo. Whether to automatically
                                        deploy new commits made to the repo
                                      type: boolean
                                    repo:
                                      description: The name of the repo in the format
                                        owner/repo. The name of the repo in the format
                                        `owner/repo`.
                                      type: string
                                  type: object
                                type: array
                              indexDocument:
                                description: The name of the index document to use
                                  when serving this static site. The name of the index
                                  document to use when serving this static site.
                                type: string
                              name:
                                description: The name of the app. Must be unique across
                                  all apps in the same account. The name of the component
                                type: string
                              outputDir:
                                description: 'An optional path to where the built
                                  assets will be located, relative to the build context.
                                  If not set, App Platform will automatically scan
                                  for these directory names: _static, dist, public.
                                  An optional path to where the built assets will
                                  be located, relative to the build context. If not
                                  set, App Platform will automatically scan for these
                                  directory names: `_static`, `dist`, `public`.'
                                type: string
                              routes:
                                description: (Deprecated - use ingress) An HTTP paths
                                  that should be routed to this component.
                                items:
                                  properties:
                                    path:
                                      description: The path to match on. Path specifies
                                        an route by HTTP path prefix. Paths must start
                                        with / and must be unique within the app.
                                      type: string
                                    preservePathPrefix:
                                      description: An optional boolean flag to preserve
                                        the path that is forwarded to the backend
                                        service. By default, the HTTP request path
                                        will be trimmed from the left when forwarded
                                        to the component. An optional flag to preserve
                                        the path that is forwarded to the backend
                                        service.
                                      type: boolean
                                  type: object
                                type: array
                              sourceDir:
                                description: An optional path to the working directory
                                  to use for the build. An optional path to the working
                                  directory to use for the build.
                                type: string
                            type: object
                          type: array
                        worker:
                          items:
                            properties:
                              alert:
                                description: Describes an alert policy for the app.
                                  Alert policies for the app component
                                items:
                                  properties:
                                    disabled:
                                      description: 'Determines whether or not the
                                        alert is disabled (default: false).'
                                      type: boolean
                                    operator:
                                      description: The operator to use. This is either
                                        of GREATER_THAN or LESS_THAN.
                                      type: string
                                    rule:
                                      description: 'The type of the alert to configure.
                                        Top-level app alert policies can be: DEPLOYMENT_FAILED,
                                        DEPLOYMENT_LIVE, DOMAIN_FAILED, or DOMAIN_LIVE.'
                                      type: string
                                    value:
                                      description: The value of the environment variable.
                                      type: number
                                    window:
                                      description: 'The time before alerts should
                                        be triggered. This is may be one of: FIVE_MINUTES,
                                        TEN_MINUTES, THIRTY_MINUTES, ONE_HOUR.'
                                      type: string
                                  type: object
                                type: array
                              buildCommand:
                                description: An optional build command to run while
                                  building this component from source. An optional
                                  build command to run while building this component
                                  from source.
                                type: string
                              dockerfilePath:
                                description: The path to a Dockerfile relative to
                                  the root of the repo. If set, overrides usage of
                                  buildpacks. The path to a Dockerfile relative to
                                  the root of the repo. If set, overrides usage of
                                  buildpacks.
                                type: string
                              env:
                                description: Describes an app-wide environment variable
                                  made available to all components.
                                items:
                                  properties:
                                    key:
                                      description: The name of the environment variable.
                                        The name of the environment variable.
                                      type: string
                                    scope:
                                      description: The visibility scope of the environment
                                        variable. One of RUN_TIME, BUILD_TIME, or
                                        RUN_AND_BUILD_TIME (default). The visibility
                                        scope of the environment variable.
                                      type: string
                                    type:
                                      description: 'The domain type, which can be
                                        one of the following: The type of the environment
                                        variable.'
                                      type: string
                                  type: object
                                type: array
                              environmentSlug:
                                description: An environment slug describing the type
                                  of this app. An environment slug describing the
                                  type of this app.
                                type: string
                              git:
                                description: A Git repo to use as the component's
                                  source. The repository must be able to be cloned
                                  without authentication.  Only one of git, github
                                  or gitlab  may be set
                                items:
                                  properties:
                                    branch:
                                      description: The name of the branch to use.
                                        The name of the branch to use.
                                      type: string
                                    repoCloneUrl:
                                      description: The clone URL of the repo. The
                                        clone URL of the repo.
                                      type: string
                                  type: object
                                type: array
                              github:
                                description: A GitHub repo to use as the component's
                                  source. DigitalOcean App Platform must have access
                                  to the repository. Only one of git, github, gitlab,
                                  or image may be set.
                                items:
                                  properties:
                                    branch:
                                      description: The name of the branch to use.
                                        The name of the branch to use.
                                      type: string
                                    deployOnPush:
                                      description: Whether to automatically deploy
                                        new commits made to the repo. Whether to automatically
                                        deploy new commits made to the repo
                                      type: boolean
                                    repo:
                                      description: The name of the repo in the format
                                        owner/repo. The name of the repo in the format
                                        `owner/repo`.
                                      type: string
                                  type: object
                                type: array
                              gitlab:
                                description: A Gitlab repo to use as the component's
                                  source. DigitalOcean App Platform must have access
                                  to the repository. Only one of git, github, gitlab,
                                  or image may be set.
                                items:
                                  properties:
                                    branch:
                                      description: The name of the branch to use.
                                        The name of the branch to use.
                                      type: string
                                    deployOnPush:
                                      description: Whether to automatically deploy
                                        new commits made to the repo. Whether to automatically
                                        deploy new commits made to the repo
                                      type: boolean
                                    repo:
                                      description: The name of the repo in the format
                                        owner/repo. The name of the repo in the format
                                        `owner/repo`.
                                      type: string
                                  type: object
                                type: array
                              image:
                                description: An image to use as the component's source.
                                  Only one of git, github, gitlab, or image may be
                                  set.
                                items:
                                  properties:
                                    deployOnPush:
                                      description: Whether to automatically deploy
                                        new commits made to the repo. Configures automatically
                                        deploying images pushed to DOCR.
                                      items:
                                        properties:
                                          enabled:
                                            description: Whether to automatically
                                              deploy images pushed to DOCR. Whether
                                              to automatically deploy images pushed
                                              to DOCR.
                                            type: boolean
                                        type: object
                                      type: array
                                    registry:
                                      description: The registry name. Must be left
                                        empty for the DOCR registry type. Required
                                        for the DOCKER_HUB registry type. The registry
                                        name. Must be left empty for the DOCR registry
                                        type.
                                      type: string
                                    registryType:
                                      description: The registry type. One of DOCR
                                        (DigitalOcean container registry) or DOCKER_HUB.
                                        The registry type.
                                      type: string
                                    repository:
                                      description: The repository name. The repository
                                        name.
                                      type: string
                                    tag:
                                      description: The repository tag. Defaults to
                                        latest if not provided. The repository tag.
                                        Defaults to latest if not provided.
                                      type: string
                                  type: object
                                type: array
                              instanceCount:
                                description: The amount of instances that this component
                                  should be scaled to. The amount of instances that
                                  this component should be scaled to.
                                type: number
                              instanceSizeSlug:
                                description: 'The instance size to use for this component.
                                  This determines the plan (basic or professional)
                                  and the available CPU and memory. The list of available
                                  instance sizes can be found with the API or using
                                  the doctl CLI (doctl apps tier instance-size list).
                                  Default: basic-xxs The instance size to use for
                                  this component.'
                                type: string
                              logDestination:
                                description: Describes a log forwarding destination.
                                  Logs
                                items:
                                  properties:
                                    datadog:
                                      description: Datadog configuration. Datadog
                                        configuration.
                                      items:
                                        properties:
                                          apiKey:
                                            description: Datadog API key. Datadog
                                              API key.
                                            type: string
                                          endpoint:
                                            description: Papertrail syslog endpoint.
                                              Datadog HTTP log intake endpoint.
                                            type: string
                                        type: object
                                      type: array
                                    logtail:
                                      description: Logtail configuration. Logtail
                                        configuration.
                                      items:
                                        properties:
                                          token:
                                            description: Logtail token. Logtail token.
                                            type: string
                                        type: object
                                      type: array
                                    name:
                                      description: The name of the app. Must be unique
                                        across all apps in the same account. Name
                                        of the log destination
                                      type: string
                                    papertrail:
                                      description: Papertrail configuration. Papertrail
                                        configuration.
                                      items:
                                        properties:
                                          endpoint:
                                            description: Papertrail syslog endpoint.
                                              Papertrail syslog endpoint.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              name:
                                description: The name of the app. Must be unique across
                                  all apps in the same account. The name of the component
                                type: string
                              runCommand:
                                description: An optional run command to override the
                                  component's default. An optional run command to
                                  override the component's default.
                                type: string
                              sourceDir:
                                description: An optional path to the working directory
                                  to use for the build. An optional path to the working
                                  directory to use for the build.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: 'THIS IS A BETA FIELD. It is on by default but can be
                  opted out through a Crossplane feature flag. ManagementPolicies
                  specify the array of actions Crossplane is allowed to take on the
                  managed and external resources. This field is planned to replace
                  the DeletionPolicy field in a future release. Currently, both could
                  be set independently and non-default values would be honored if
                  the feature flag is enabled. If both are custom, the DeletionPolicy
                  field will be ignored. See the design doc for more information:
                  https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md'
                items:
                  description: A ManagementAction represents an action that the Crossplane
                    controllers can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: ProviderConfigReference specifies how the provider that
                  will be used to create, observe, update, and delete this managed
                  resource should be configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: Resolution specifies whether resolution of this
                          reference is required. The default is 'Required', which
                          means the reconcile will fail if the reference cannot be
                          resolved. 'Optional' means this reference will be a no-op
                          if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: Resolve specifies when this reference should
                          be resolved. The default is 'IfNotPresent', which will attempt
                          to resolve the reference only when the corresponding field
                          is not present. Use 'Always' to resolve the reference on
                          every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              publishConnectionDetailsTo:
                description: PublishConnectionDetailsTo specifies the connection secret
                  config which contains a name, metadata and a reference to secret
                  store config to which any connection details for this managed resource
                  should be written. Connection details frequently include the endpoint,
                  username, and password required to connect to the managed resource.
                properties:
                  configRef:
                    default:
                      name: default
                    description: SecretStoreConfigRef specifies which secret store
                      config should be used for this ConnectionSecret.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: Resolution specifies whether resolution of
                              this reference is required. The default is 'Required',
                              which means the reconcile will fail if the reference
                              cannot be resolved. 'Optional' means this reference
                              will be a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: Resolve specifies when this reference should
                              be resolved. The default is 'IfNotPresent', which will
                              attempt to resolve the reference only when the corresponding
                              field is not present. Use 'Always' to resolve the reference
                              on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  metadata:
                    description: Metadata is the metadata for connection secret.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations are the annotations to be added to
                          connection secret. - For Kubernetes secrets, this will be
                          used as "metadata.annotations". - It is up to Secret Store
                          implementation for others store types.
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: Labels are the labels/tags to be added to connection
                          secret. - For Kubernetes secrets, this will be used as "metadata.labels".
                          - It is up to Secret Store implementation for others store
                          types.
                        type: object
                      type:
                        description: Type is the SecretType for the connection secret.
                          - Only valid for Kubernetes Secret Stores.
                        type: string
                    type: object
                  name:
                    description: Name is the name of the connection secret.
                    type: string
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: WriteConnectionSecretToReference specifies the namespace
                  and name of a Secret to which any connection details for this managed
                  resource should be written. Connection details frequently include
                  the endpoint, username, and password required to connect to the
                  managed resource. This field is planned to be replaced in a future
                  release in favor of PublishConnectionDetailsTo. Currently, both
                  could be set independently and connection details would be published
                  to both without affecting each other.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: AppStatus defines the observed state of App.
            properties:
              atProvider:
                properties:
                  activeDeploymentId:
                    description: The ID the app's currently active deployment. The
                      ID the App's currently active deployment
                    type: string
                  createdAt:
                    description: The date and time of when the app was created. The
                      date and time of when the App was created
                    type: string
                  defaultIngress:
                    description: The default URL to access the app. The default URL
                      to access the App
                    type: string
                  id:
                    description: The ID of the app.
                    type: string
                  liveUrl:
                    description: The live URL of the app.
                    type: string
                  projectId:
                    description: The ID of the project that the app is assigned to.
                    type: string
                  spec:
                    description: A DigitalOcean App spec describing the app. A DigitalOcean
                      App Platform Spec
                    items:
                      properties:
                        alert:
                          description: Describes an alert policy for the app.
                          items:
                            properties:
                              disabled:
                                description: 'Determines whether or not the alert
                                  is disabled (default: false).'
                                type: boolean
                              rule:
                                description: 'The type of the alert to configure.
                                  Top-level app alert policies can be: DEPLOYMENT_FAILED,
                                  DEPLOYMENT_LIVE, DOMAIN_FAILED, or DOMAIN_LIVE.'
                                type: string
                            type: object
                          type: array
                        database:
                          items:
                            properties:
                              clusterName:
                                description: The name of the underlying DigitalOcean
                                  DBaaS cluster. This is required for production databases.
                                  For dev databases, if cluster_name is not set, a
                                  new cluster will be provisioned. The name of the
                                  underlying DigitalOcean DBaaS cluster. This is required
                                  for production databases. For dev databases, if
                                  cluster_name is not set, a new cluster will be provisioned.
                                type: string
                              dbName:
                                description: The name of the MySQL or PostgreSQL database
                                  to configure. The name of the MySQL or PostgreSQL
                                  database to configure.
                                type: string
                              dbUser:
                                description: The name of the MySQL or PostgreSQL user
                                  to configure. The name of the MySQL or PostgreSQL
                                  user to configure.
                                type: string
                              engine:
                                description: The database engine to use (MYSQL, PG,
                                  REDIS, or MONGODB). The database engine to use.
                                type: string
                              name:
                                description: The name of the app. Must be unique across
                                  all apps in the same account. The name of the component
                                type: string
                              production:
                                description: Whether this is a production or dev database.
                                  Whether this is a production or dev database.
                                type: boolean
                              version:
                                description: The version of the database engine. The
                                  version of the database engine.
                                type: string
                            type: object
                          type: array
                        domain:
                          description: Describes a domain where the application will
                            be made available.
                          items:
                            properties:
                              name:
                                description: The name of the app. Must be unique across
                                  all apps in the same account. The hostname for the
                                  domain.
                                type: string
                              type:
                                description: 'The domain type, which can be one of
                                  the following: The type of the domain.'
                                type: string
                              wildcard:
                                description: A boolean indicating whether the domain
                                  includes all sub-domains, in addition to the given
                                  domain. Indicates whether the domain includes all
                                  sub-domains, in addition to the given domain.
                                type: boolean
                              zone:
                                description: If the domain uses DigitalOcean DNS and
                                  you would like App Platform to automatically manage
                                  it for you, set this to the name of the domain on
                                  your account. If the domain uses DigitalOcean DNS
                                  and you would like App Platform to automatically
                                  manage it for you, set this to the name of the domain
                                  on your account.
                                type: string
                            type: object
                          type: array
                        domains:
                          items:
                            type: string
                          type: array
                        env:
                          description: Describes an app-wide environment variable
                            made available to all components.
                          items:
                            properties:
                              key:
                                description: The name of the environment variable.
                                  The name of the environment variable.
                                type: string
                              scope:
                                description: The visibility scope of the environment
                                  variable. One of RUN_TIME, BUILD_TIME, or RUN_AND_BUILD_TIME
                                  (default). The visibility scope of the environment
                                  variable.
                                type: string
                              type:
                                description: 'The domain type, which can be one of
                                  the following: The type of the environment variable.'
                                type: string
                            type: object
                          type: array
                        features:
                          description: A list of the features applied to the app.
                            The default buildpack can be overridden here. List of
                            available buildpacks can be found using the doctl CLI
                            List of features which is applied to the app
                          items:
                            type: string
                          type: array
                        function:
                          items:
                            properties:
                              alert:
                                description: Describes an alert policy for the app.
                                  Alert policies for the app component
                                items:
                                  properties:
                                    disabled:
                                      description: 'Determines whether or not the
                                        alert is disabled (default: false).'
                                      type: boolean
                                    operator:
                                      description: The operator to use. This is either
                                        of GREATER_THAN or LESS_THAN.
                                      type: string
                                    rule:
                                      description: 'The type of the alert to configure.
                                        Top-level app alert policies can be: DEPLOYMENT_FAILED,
                                        DEPLOYMENT_LIVE, DOMAIN_FAILED, or DOMAIN_LIVE.'
                                      type: string
                                    value:
                                      description: The value of the environment variable.
                                      type: number
                                    window:
                                      description: 'The time before alerts should
                                        be triggered. This is may be one of: FIVE_MINUTES,
                                        TEN_MINUTES, THIRTY_MINUTES, ONE_HOUR.'
                                      type: string
                                  type: object
                                type: array
                              cors:
                                description: The CORS policies of the app.
                                items:
                                  properties:
                                    allowCredentials:
                                      description: Whether browsers should expose
                                        the response to the client-side JavaScript
                                        code when the request's credentials mode is
                                        include. This configures the Access-Control-Allow-Credentials
                                        header. Whether browsers should expose the
                                        response to the client-side JavaScript code
                                        when the request’s credentials mode is `include`.
                                        This configures the Access-Control-Allow-Credentials
                                        header.
                                      type: boolean
                                    allowHeaders:
                                      description: The set of allowed HTTP request
                                        headers. This configures the Access-Control-Allow-Headers
                                        header. The set of allowed HTTP request headers.
                                        This configures the Access-Control-Allow-Headers
                                        header.
                                      items:
                                        type: string
                                      type: array
                                    allowMethods:
                                      description: The set of allowed HTTP methods.
                                        This configures the Access-Control-Allow-Methods
                                        header. The set of allowed HTTP methods. This
                                        configures the Access-Control-Allow-Methods
                                        header.
                                      items:
                                        type: string
                                      type: array
                                    allowOrigins:
                                      description: The Access-Control-Allow-Origin
                                        can be The set of allowed CORS origins. This
                                        configures the Access-Control-Allow-Origin
                                        header.
                                      items:
                                        properties:
                                          exact:
                                            description: The Access-Control-Allow-Origin
                                              header will be set to the client's origin
                                              only if the client's origin exactly
                                              matches the value you provide. Exact
                                              string match.
                                            type: string
                                          prefix:
                                            description: Prefix-based match. Prefix-based
                                              match.
                                            type: string
                                          regex:
                                            description: The Access-Control-Allow-Origin
                                              header will be set to the client's origin
                                              if the client’s origin matches the regex
                                              you provide, in RE2 style syntax. RE2
                                              style regex-based match.
                                            type: string
                                        type: object
                                      type: array
                                    exposeHeaders:
                                      description: The set of HTTP response headers
                                        that browsers are allowed to access. This
                                        configures the Access-Control-Expose-Headers
                                        header. The set of HTTP response headers that
                                        browsers are allowed to access. This configures
                                        the Access-Control-Expose-Headers header.
                                      items:
                                        type: string
                                      type: array
                                    maxAge:
                                      description: 'An optional duration specifying
                                        how long browsers can cache the results of
                                        a preflight request. This configures the Access-Control-Max-Age
                                        header. Example: 5h30m. An optional duration
                                        specifying how long browsers can cache the
                                        results of a preflight request. This configures
                                        the Access-Control-Max-Age header. Example:
                                        `5h30m`.'
                                      type: string
                                  type: object
                                type: array
                              env:
                                description: Describes an app-wide environment variable
                                  made available to all components.
                                items:
                                  properties:
                                    key:
                                      description: The name of the environment variable.
                                        The name of the environment variable.
                                      type: string
                                    scope:
                                      description: The visibility scope of the environment
                                        variable. One of RUN_TIME, BUILD_TIME, or
                                        RUN_AND_BUILD_TIME (default). The visibility
                                        scope of the environment variable.
                                      type: string
                                    type:
                                      description: 'The domain type, which can be
                                        one of the following: The type of the environment
                                        variable.'
                                      type: string
                                  type: object
                                type: array
                              git:
                                description: A Git repo to use as the component's
                                  source. The repository must be able to be cloned
                                  without authentication.  Only one of git, github
                                  or gitlab  may be set
                                items:
                                  properties:
                                    branch:
                                      description: The name of the branch to use.
                                        The name of the branch to use.
                                      type: string
                                    repoCloneUrl:
                                      description: The clone URL of the repo. The
                                        clone URL of the repo.
                                      type: string
                                  type: object
                                type: array
                              github:
                                description: A GitHub repo to use as the component's
                                  source. DigitalOcean App Platform must have access
                                  to the repository. Only one of git, github, gitlab,
                                  or image may be set.
                                items:
                                  properties:
                                    branch:
                                      description: The name of the branch to use.
                                        The name of the branch to use.
                                      type: string
                                    deployOnPush:
                                      description: Whether to automatically deploy
                                        new commits made to the repo. Whether to automatically
                                        deploy new commits made to the repo
                                      type: boolean
                                    repo:
                                      description: The name of the repo in the format
                                        owner/repo. The name of the repo in the format
                                        `owner/repo`.
                                      type: string
                                  type: object
                                type: array
                              gitlab:
                                description: A Gitlab repo to use as the component's
                                  source. DigitalOcean App Platform must have access
                                  to the repository. Only one of git, github, gitlab,
                                  or image may be set.
                                items:
                                  properties:
                                    branch:
                                      description: The name of the branch to use.
                                        The name of the branch to use.
                                      type: string
                                    deployOnPush:
                                      description: Whether to automatically deploy
                                        new commits made to the repo. Whether to automatically
                                        deploy new commits made to the repo
                                      type: boolean
                                    repo:
                                      description: The name of the repo in the format
                                        owner/repo. The name of the repo in the format
                                        `owner/repo`.
                                      type: string
                                  type: object
                                type: array
                              logDestination:
                                description: Describes a log forwarding destination.
                                  Logs
                                items:
                                  properties:
                                    datadog:
                                      description: Datadog configuration. Datadog
                                        configuration.
                                      items:
                                        properties:
                                          apiKey:
                                            description: Datadog API key. Datadog
                                              API key.
                                            type: string
                                          endpoint:
                                            description: Papertrail syslog endpoint.
                                              Datadog HTTP log intake endpoint.
                                            type: string
                                        type: object
                                      type: array
                                    logtail:
                                      description: Logtail configuration. Logtail
                                        configuration.
                                      items:
                                        properties:
                                          token:
                                            description: Logtail token. Logtail token.
                                            type: string
                                        type: object
                                      type: array
                                    name:
                                      description: The name of the app. Must be unique
                                        across all apps in the same account. Name
                                        of the log destination
                                      type: string
                                    papertrail:
                                      description: Papertrail configuration. Papertrail
                                        configuration.
                                      items:
                                        properties:
                                          endpoint:
                                            description: Papertrail syslog endpoint.
                                              Papertrail syslog endpoint.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              name:
                                description: The name of the app. Must be unique across
                                  all apps in the same account. The name of the component
                                type: string
                              routes:
                                description: (Deprecated - use ingress) An HTTP paths
                                  that should be routed to this component.
                                items:
                                  properties:
                                    path:
                                      description: The path to match on. Path specifies
                                        an route by HTTP path prefix. Paths must start
                                        with / and must be unique within the app.
                                      type: string
                                    preservePathPrefix:
                                      description: An optional boolean flag to preserve
                                        the path that is forwarded to the backend
                                        service. By default, the HTTP request path
                                        will be trimmed from the left when forwarded
                                        to the component. An optional flag to preserve
                                        the path that is forwarded to the backend
                                        service.
                                      type: boolean
                                  type: object
                                type: array
                              sourceDir:
                                description: An optional path to the working directory
                                  to use for the build. An optional path to the working
                                  directory to use for the build.
                                type: string
                            type: object
                          type: array
                        ingress:
                          description: Specification for component routing, rewrites,
                            and redirects.
                          items:
                            properties:
                              rule:
                                description: 'The type of the alert to configure.
                                  Top-level app alert policies can be: DEPLOYMENT_FAILED,
                                  DEPLOYMENT_LIVE, DOMAIN_FAILED, or DOMAIN_LIVE.'
                                items:
                                  properties:
                                    component:
                                      description: The component to route to. Only
                                        one of component or redirect may be set.
                                      items:
                                        properties:
                                          name:
                                            description: The name of the app. Must
                                              be unique across all apps in the same
                                              account.
                                            type: string
                                          preservePathPrefix:
                                            description: An optional boolean flag
                                              to preserve the path that is forwarded
                                              to the backend service. By default,
                                              the HTTP request path will be trimmed
                                              from the left when forwarded to the
                                              component.
                                            type: boolean
                                          rewrite:
                                            description: An optional field that will
                                              rewrite the path of the component to
                                              be what is specified here. This is mutually
                                              exclusive with preserve_path_prefix.
                                            type: string
                                        type: object
                                      type: array
                                    cors:
                                      description: The CORS policies of the app.
                                      items:
                                        properties:
                                          allowCredentials:
                                            description: Whether browsers should expose
                                              the response to the client-side JavaScript
                                              code when the request's credentials
                                              mode is include. This configures the
                                              Access-Control-Allow-Credentials header.
                                              Whether browsers should expose the response
                                              to the client-side JavaScript code when
                                              the request’s credentials mode is `include`.
                                              This configures the Access-Control-Allow-Credentials
                                              header.
                                            type: boolean
                                          allowHeaders:
                                            description: The set of allowed HTTP request
                                              headers. This configures the Access-Control-Allow-Headers
                                              header. The set of allowed HTTP request
                                              headers. This configures the Access-Control-Allow-Headers
                                              header.
                                            items:
                                              type: string
                                            type: array
                                          allowMethods:
                                            description: The set of allowed HTTP methods.
                                              This configures the Access-Control-Allow-Methods
                                              header. The set of allowed HTTP methods.
                                              This configures the Access-Control-Allow-Methods
                                              header.
                                            items:
                                              type: string
                                            type: array
                                          allowOrigins:
                                            description: The Access-Control-Allow-Origin
                                              can be The set of allowed CORS origins.
                                              This configures the Access-Control-Allow-Origin
                                              header.
                                            items:
                                              properties:
                                                exact:
                                                  description: The Access-Control-Allow-Origin
                                                    header will be set to the client's
                                                    origin only if the client's origin
                                                    exactly matches the value you
                                                    provide. Exact string match.
                                                  type: string
                                                prefix:
                                                  description: Prefix-based match.
                                                    Prefix-based match.
                                                  type: string
                                                regex:
                                                  description: The Access-Control-Allow-Origin
                                                    header will be set to the client's
                                                    origin if the client’s origin
                                                    matches the regex you provide,
                                                    in RE2 style syntax. RE2 style
                                                    regex-based match.
                                                  type: string
                                              type: object
                                            type: array
                                          exposeHeaders:
                                            description: The set of HTTP response
                                              headers that browsers are allowed to
                                              access. This configures the Access-Control-Expose-Headers
                                              header. The set of HTTP response headers
                                              that browsers are allowed to access.
                                              This configures the Access-Control-Expose-Headers
                                              header.
                                            items:
                                              type: string
                                            type: array
                                          maxAge:
                                            description: 'An optional duration specifying
                                              how long browsers can cache the results
                                              of a preflight request. This configures
                                              the Access-Control-Max-Age header. Example:
                                              5h30m. An optional duration specifying
                                              how long browsers can cache the results
                                              of a preflight request. This configures
                                              the Access-Control-Max-Age header. Example:
                                              `5h30m`.'
                                            type: string
                                        type: object
                                      type: array
                                    match:
                                      description: The match configuration for the
                                        rule
                                      items:
                                        properties:
                                          path:
                                            description: The path to match on.
                                            items:
                                              properties:
                                                prefix:
                                                  description: Prefix-based match.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    redirect:
                                      description: The redirect configuration for
                                        the rule. Only one of component or redirect
                                        may be set.
                                      items:
                                        properties:
                                          authority:
                                            description: The authority/host to redirect
                                              to. This can be a hostname or IP address.
                                            type: string
                                          port:
                                            description: The port to redirect to.
                                            type: number
                                          redirectCode:
                                            description: The redirect code to use.
                                              Supported values are 300, 301, 302,
                                              303, 304, 307, 308.
                                            type: number
                                          scheme:
                                            description: The scheme to redirect to.
                                              Supported values are http or https
                                            type: string
                                          uri:
                                            description: An optional URI path to redirect
                                              to.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                            type: object
                          type: array
                        job:
                          items:
                            properties:
                              alert:
                                description: Describes an alert policy for the app.
                                  Alert policies for the app component
                                items:
                                  properties:
                                    disabled:
                                      description: 'Determines whether or not the
                                        alert is disabled (default: false).'
                                      type: boolean
                                    operator:
                                      description: The operator to use. This is either
                                        of GREATER_THAN or LESS_THAN.
                                      type: string
                                    rule:
                                      description: 'The type of the alert to configure.
                                        Top-level app alert policies can be: DEPLOYMENT_FAILED,
                                        DEPLOYMENT_LIVE, DOMAIN_FAILED, or DOMAIN_LIVE.'
                                      type: string
                                    value:
                                      description: The value of the environment variable.
                                      type: number
                                    window:
                                      description: 'The time before alerts should
                                        be triggered. This is may be one of: FIVE_MINUTES,
                                        TEN_MINUTES, THIRTY_MINUTES, ONE_HOUR.'
                                      type: string
                                  type: object
                                type: array
                              buildCommand:
                                description: An optional build command to run while
                                  building this component from source. An optional
                                  build command to run while building this component
                                  from source.
                                type: string
                              dockerfilePath:
                                description: The path to a Dockerfile relative to
                                  the root of the repo. If set, overrides usage of
                                  buildpacks. The path to a Dockerfile relative to
                                  the root of the repo. If set, overrides usage of
                                  buildpacks.
                                type: string
                              env:
                                description: Describes an app-wide environment variable
                                  made available to all components.
                                items:
                                  properties:
                                    key:
                                      description: The name of the environment variable.
                                        The name of the environment variable.
                                      type: string
                                    scope:
                                      description: The visibility scope of the environment
                                        variable. One of RUN_TIME, BUILD_TIME, or
                                        RUN_AND_BUILD_TIME (default). The visibility
                                        scope of the environment variable.
                                      type: string
                                    type:
                                      description: 'The domain type, which can be
                                        one of the following: The type of the environment
                                        variable.'
                                      type: string
                                  type: object
                                type: array
                              environmentSlug:
                                description: An environment slug describing the type
                                  of this app. An environment slug describing the
                                  type of this app.
                                type: string
                              git:
                                description: A Git repo to use as the component's
                                  source. The repository must be able to be cloned
                                  without authentication.  Only one of git, github
                                  or gitlab  may be set
                                items:
                                  properties:
                                    branch:
                                      description: The name of the branch to use.
                                        The name of the branch to use.
                                      type: string
                                    repoCloneUrl:
                                      description: The clone URL of the repo. The
                                        clone URL of the repo.
                                      type: string
                                  type: object
                                type: array
                              github:
                                description: A GitHub repo to use as the component's
                                  source. DigitalOcean App Platform must have access
                                  to the repository. Only one of git, github, gitlab,
                                  or image may be set.
                                items:
                                  properties:
                                    branch:
                                      description: The name of the branch to use.
                                        The name of the branch to use.
                                      type: string
                                    deployOnPush:
                                      description: Whether to automatically deploy
                                        new commits made to the repo. Whether to automatically
                                        deploy new commits made to the repo
                                      type: boolean
                                    repo:
                                      description: The name of the repo in the format
                                        owner/repo. The name of the repo in the format
                                        `owner/repo`.
                                      type: string
                                  type: object
                                type: array
                              gitlab:
                                description: A Gitlab repo to use as the component's
                                  source. DigitalOcean App Platform must have access
                                  to the repository. Only one of git, github, gitlab,
                                  or image may be set.
                                items:
                                  properties:
                                    branch:
                                      description: The name of the branch to use.
                                        The name of the branch to use.
                                      type: string
                                    deployOnPush:
                                      description: Whether to automatically deploy
                                        new commits made to the repo. Whether to automatically
                                        deploy new commits made to the repo
                                      type: boolean
                                    repo:
                                      description: The name of the repo in the format
                                        owner/repo. The name of the repo in the format
                                        `owner/repo`.
                                      type: string
                                  type: object
                                type: array
                              image:
                                description: An image to use as the component's source.
                                  Only one of git, github, gitlab, or image may be
                                  set.
                                items:
                                  properties:
                                    deployOnPush:
                                      description: Whether to automatically deploy
                                        new commits made to the repo. Configures automatically
                                        deploying images pushed to DOCR.
                                      items:
                                        properties:
                                          enabled:
                                            description: Whether to automatically
                                              deploy images pushed to DOCR. Whether
                                              to automatically deploy images pushed
                                              to DOCR.
                                            type: boolean
                                        type: object
                                      type: array
                                    registry:
                                      description: The registry name. Must be left
                                        empty for the DOCR registry type. Required
                                        for the DOCKER_HUB registry type. The registry
                                        name. Must be left empty for the DOCR registry
                                        type.
                                      type: string
                                    registryType:
                                      description: The registry type. One of DOCR
                                        (DigitalOcean container registry) or DOCKER_HUB.
                                        The registry type.
                                      type: string
                                    repository:
                                      description: The repository name. The repository
                                        name.
                                      type: string
                                    tag:
                                      description: The repository tag. Defaults to
                                        latest if not provided. The repository tag.
                                        Defaults to latest if not provided.
                                      type: string
                                  type: object
                                type: array
                              instanceCount:
                                description: The amount of instances that this component
                                  should be scaled to. The amount of instances that
                                  this component should be scaled to.
                                type: number
                              instanceSizeSlug:
                                description: 'The instance size to use for this component.
                                  This determines the plan (basic or professional)
                                  and the available CPU and memory. The list of available
                                  instance sizes can be found with the API or using
                                  the doctl CLI (doctl apps tier instance-size list).
                                  Default: basic-xxs The instance size to use for
                                  this component.'
                                type: string
                              kind:
                                description: 'The type of job and when it will be
                                  run during the deployment process. It may be one
                                  of: The type of job and when it will be run during
                                  the deployment process.'
                                type: string
                              logDestination:
                                description: Describes a log forwarding destination.
                                  Logs
                                items:
                                  properties:
                                    datadog:
                                      description: Datadog configuration. Datadog
                                        configuration.
                                      items:
                                        properties:
                                          apiKey:
                                            description: Datadog API key. Datadog
                                              API key.
                                            type: string
                                          endpoint:
                                            description: Papertrail syslog endpoint.
                                              Datadog HTTP log intake endpoint.
                                            type: string
                                        type: object
                                      type: array
                                    logtail:
                                      description: Logtail configuration. Logtail
                                        configuration.
                                      items:
                                        properties:
                                          token:
                                            description: Logtail token. Logtail token.
                                            type: string
                                        type: object
                                      type: array
                                    name:
                                      description: The name of the app. Must be unique
                                        across all apps in the same account. Name
                                        of the log destination
                                      type: string
                                    papertrail:
                                      description: Papertrail configuration. Papertrail
                                        configuration.
                                      items:
                                        properties:
                                          endpoint:
                                            description: Papertrail syslog endpoint.
                                              Papertrail syslog endpoint.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              name:
                                description: The name of the app. Must be unique across
                                  all apps in the same account. The name of the component
                                type: string
                              runCommand:
                                description: An optional run command to override the
                                  component's default. An optional run command to
                                  override the component's default.
                                type: string
                              sourceDir:
                                description: An optional path to the working directory
                                  to use for the build. An optional path to the working
                                  directory to use for the build.
                                type: string
                            type: object
                          type: array
                        name:
                          description: The name of the app. Must be unique across
                            all apps in the same account. The name of the app. Must
                            be unique across all apps in the same account.
                          type: string
                        region:
                          description: The slug for the DigitalOcean data center region
                            hosting the app. The slug for the DigitalOcean data center
                            region hosting the app
                          type: string
                        service:
                          items:
                            properties:
                              alert:
                                description: Describes an alert policy for the app.
                                  Alert policies for the app component
                                items:
                                  properties:
                                    disabled:
                                      description: 'Determines whether or not the
                                        alert is disabled (default: false).'
                                      type: boolean
                                    operator:
                                      description: The operator to use. This is either
                                        of GREATER_THAN or LESS_THAN.
                                      type: string
                                    rule:
                                      description: 'The type of the alert to configure.
                                        Top-level app alert policies can be: DEPLOYMENT_FAILED,
                                        DEPLOYMENT_LIVE, DOMAIN_FAILED, or DOMAIN_LIVE.'
                                      type: string
                                    value:
                                      description: The value of the environment variable.
                                      type: number
                                    window:
                                      description: 'The time before alerts should
                                        be triggered. This is may be one of: FIVE_MINUTES,
                                        TEN_MINUTES, THIRTY_MINUTES, ONE_HOUR.'
                                      type: string
                                  type: object
                                type: array
                              buildCommand:
                                description: An optional build command to run while
                                  building this component from source. An optional
                                  build command to run while building this component
                                  from source.
                                type: string
                              cors:
                                description: The CORS policies of the app.
                                items:
                                  properties:
                                    allowCredentials:
                                      description: Whether browsers should expose
                                        the response to the client-side JavaScript
                                        code when the request's credentials mode is
                                        include. This configures the Access-Control-Allow-Credentials
                                        header. Whether browsers should expose the
                                        response to the client-side JavaScript code
                                        when the request’s credentials mode is `include`.
                                        This configures the Access-Control-Allow-Credentials
                                        header.
                                      type: boolean
                                    allowHeaders:
                                      description: The set of allowed HTTP request
                                        headers. This configures the Access-Control-Allow-Headers
                                        header. The set of allowed HTTP request headers.
                                        This configures the Access-Control-Allow-Headers
                                        header.
                                      items:
                                        type: string
                                      type: array
                                    allowMethods:
                                      description: The set of allowed HTTP methods.
                                        This configures the Access-Control-Allow-Methods
                                        header. The set of allowed HTTP methods. This
                                        configures the Access-Control-Allow-Methods
                                        header.
                                      items:
                                        type: string
                                      type: array
                                    allowOrigins:
                                      description: The Access-Control-Allow-Origin
                                        can be The set of allowed CORS origins. This
                                        configures the Access-Control-Allow-Origin
                                        header.
                                      items:
                                        properties:
                                          exact:
                                            description: The Access-Control-Allow-Origin
                                              header will be set to the client's origin
                                              only if the client's origin exactly
                                              matches the value you provide. Exact
                                              string match.
                                            type: string
                                          prefix:
                                            description: Prefix-based match. Prefix-based
                                              match.
                                            type: string
                                          regex:
                                            description: The Access-Control-Allow-Origin
                                              header will be set to the client's origin
                                              if the client’s origin matches the regex
                                              you provide, in RE2 style syntax. RE2
                                              style regex-based match.
                                            type: string
                                        type: object
                                      type: array
                                    exposeHeaders:
                                      description: The set of HTTP response headers
                                        that browsers are allowed to access. This
                                        configures the Access-Control-Expose-Headers
                                        header. The set of HTTP response headers that
                                        browsers are allowed to access. This configures
                                        the Access-Control-Expose-Headers header.
                                      items:
                                        type: string
                                      type: array
                                    maxAge:
                                      description: 'An optional duration specifying
                                        how long browsers can cache the results of
                                        a preflight request. This configures the Access-Control-Max-Age
                                        header. Example: 5h30m. An optional duration
                                        specifying how long browsers can cache the
                                        results of a preflight request. This configures
                                        the Access-Control-Max-Age header. Example:
                                        `5h30m`.'
                                      type: string
                                  type: object
                                type: array
                              dockerfilePath:
                                description: The path to a Dockerfile relative to
                                  the root of the repo. If set, overrides usage of
                                  buildpacks. The path to a Dockerfile relative to
                                  the root of the repo. If set, overrides usage of
                                  buildpacks.
                                type: string
                              env:
                                description: Describes an app-wide environment variable
                                  made available to all components.
                                items:
                                  properties:
                                    key:
                                      description: The name of the environment variable.
                                        The name of the environment variable.
                                      type: string
                                    scope:
                                      description: The visibility scope of the environment
                                        variable. One of RUN_TIME, BUILD_TIME, or
                                        RUN_AND_BUILD_TIME (default). The visibility
                                        scope of the environment variable.
                                      type: string
                                    type:
                                      description: 'The domain type, which can be
                                        one of the following: The type of the environment
                                        variable.'
                                      type: string
                                  type: object
                                type: array
                              environmentSlug:
                                description: An environment slug describing the type
                                  of this app. An environment slug describing the
                                  type of this app.
                                type: string
                              git:
                                description: A Git repo to use as the component's
                                  source. The repository must be able to be cloned
                                  without authentication.  Only one of git, github
                                  or gitlab  may be set
                                items:
                                  properties:
                                    branch:
                                      description: The name of the branch to use.
                                        The name of the branch to use.
                                      type: string
                                    repoCloneUrl:
                                      description: The clone URL of the repo. The
                                        clone URL of the repo.
                                      type: string
                                  type: object
                                type: array
                              github:
                                description: A GitHub repo to use as the component's
                                  source. DigitalOcean App Platform must have access
                                  to the repository. Only one of git, github, gitlab,
                                  or image may be set.
                                items:
                                  properties:
                                    branch:
                                      description: The name of the branch to use.
                                        The name of the branch to use.
                                      type: string
                                    deployOnPush:
                                      description: Whether to automatically deploy
                                        new commits made to the repo. Whether to automatically
                                        deploy new commits made to the repo
                                      type: boolean
                                    repo:
                                      description: The name of the repo in the format
                                        owner/repo. The name of the repo in the format
                                        `owner/repo`.
                                      type: string
                                  type: object
                                type: array
                              gitlab:
                                description: A Gitlab repo to use as the component's
                                  source. DigitalOcean App Platform must have access
                                  to the repository. Only one of git, github, gitlab,
                                  or image may be set.
                                items:
                                  properties:
                                    branch:
                                      description: The name of the branch to use.
                                        The name of the branch to use.
                                      type: string
                                    deployOnPush:
                                      description: Whether to automatically deploy
                                        new commits made to the repo. Whether to automatically
                                        deploy new commits made to the repo
                                      type: boolean
                                    repo:
                                      description: The name of the repo in the format
                                        owner/repo. The name of the repo in the format
                                        `owner/repo`.
                                      type: string
                                  type: object
                                type: array
                              healthCheck:
                                description: A health check to determine the availability
                                  of this component.
                                items:
                                  properties:
                                    failureThreshold:
                                      description: The number of failed health checks
                                        before considered unhealthy. The number of
                                        failed health checks before considered unhealthy.
                                      type: number
                                    httpPath:
                                      description: The route path used for the HTTP
                                        health check ping. The route path used for
                                        the HTTP health check ping.
                                      type: string
                                    initialDelaySeconds:
                                      description: The number of seconds to wait before
                                        beginning health checks. The number of seconds
                                        to wait before beginning health checks.
                                      type: number
                                    periodSeconds:
                                      description: The number of seconds to wait between
                                        health checks. The number of seconds to wait
                                        between health checks.
                                      type: number
                                    port:
                                      description: The port to redirect to. The port
                                        on which the health check will be performed.
                                        If not set, the health check will be performed
                                        on the component's http_port.
                                      type: number
                                    successThreshold:
                                      description: The number of successful health
                                        checks before considered healthy. The number
                                        of successful health checks before considered
                                        healthy.
                                      type: number
                                    timeoutSeconds:
                                      description: The number of seconds after which
                                        the check times out. The number of seconds
                                        after which the check times out.
                                      type: number
                                  type: object
                                type: array
                              httpPort:
                                description: The internal port on which this service's
                                  run command will listen. The internal port on which
                                  this service's run command will listen.
                                type: number
                              image:
                                description: An image to use as the component's source.
                                  Only one of git, github, gitlab, or image may be
                                  set.
                                items:
                                  properties:
                                    deployOnPush:
                                      description: Whether to automatically deploy
                                        new commits made to the repo. Configures automatically
                                        deploying images pushed to DOCR.
                                      items:
                                        properties:
                                          enabled:
                                            description: Whether to automatically
                                              deploy images pushed to DOCR. Whether
                                              to automatically deploy images pushed
                                              to DOCR.
                                            type: boolean
                                        type: object
                                      type: array
                                    registry:
                                      description: The registry name. Must be left
                                        empty for the DOCR registry type. Required
                                        for the DOCKER_HUB registry type. The registry
                                        name. Must be left empty for the DOCR registry
                                        type.
                                      type: string
                                    registryType:
                                      description: The registry type. One of DOCR
                                        (DigitalOcean container registry) or DOCKER_HUB.
                                        The registry type.
                                      type: string
                                    repository:
                                      description: The repository name. The repository
                                        name.
                                      type: string
                                    tag:
                                      description: The repository tag. Defaults to
                                        latest if not provided. The repository tag.
                                        Defaults to latest if not provided.
                                      type: string
                                  type: object
                                type: array
                              instanceCount:
                                description: The amount of instances that this component
                                  should be scaled to. The amount of instances that
                                  this component should be scaled to.
                                type: number
                              instanceSizeSlug:
                                description: 'The instance size to use for this component.
                                  This determines the plan (basic or professional)
                                  and the available CPU and memory. The list of available
                                  instance sizes can be found with the API or using
                                  the doctl CLI (doctl apps tier instance-size list).
                                  Default: basic-xxs The instance size to use for
                                  this component.'
                                type: string
                              internalPorts:
                                description: A list of ports on which this service
                                  will listen for internal traffic.
                                items:
                                  type: number
                                type: array
                              logDestination:
                                description: Describes a log forwarding destination.
                                  Logs
                                items:
                                  properties:
                                    datadog:
                                      description: Datadog configuration. Datadog
                                        configuration.
                                      items:
                                        properties:
                                          apiKey:
                                            description: Datadog API key. Datadog
                                              API key.
                                            type: string
                                          endpoint:
                                            description: Papertrail syslog endpoint.
                                              Datadog HTTP log intake endpoint.
                                            type: string
                                        type: object
                                      type: array
                                    logtail:
                                      description: Logtail configuration. Logtail
                                        configuration.
                                      items:
                                        properties:
                                          token:
                                            description: Logtail token. Logtail token.
                                            type: string
                                        type: object
                                      type: array
                                    name:
                                      description: The name of the app. Must be unique
                                        across all apps in the same account. Name
                                        of the log destination
                                      type: string
                                    papertrail:
                                      description: Papertrail configuration. Papertrail
                                        configuration.
                                      items:
                                        properties:
                                          endpoint:
                                            description: Papertrail syslog endpoint.
                                              Papertrail syslog endpoint.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              name:
                                description: The name of the app. Must be unique across
                                  all apps in the same account. The name of the component
                                type: string
                              routes:
                                description: (Deprecated - use ingress) An HTTP paths
                                  that should be routed to this component.
                                items:
                                  properties:
                                    path:
                                      description: The path to match on. Path specifies
                                        an route by HTTP path prefix. Paths must start
                                        with / and must be unique within the app.
                                      type: string
                                    preservePathPrefix:
                                      description: An optional boolean flag to preserve
                                        the path that is forwarded to the backend
                                        service. By default, the HTTP request path
                                        will be trimmed from the left when forwarded
                                        to the component. An optional flag to preserve
                                        the path that is forwarded to the backend
                                        service.
                                      type: boolean
                                  type: object
                                type: array
                              runCommand:
                                description: An optional run command to override the
                                  component's default. An optional run command to
                                  override the component's default.
                                type: string
                              sourceDir:
                                description: An optional path to the working directory
                                  to use for the build. An optional path to the working
                                  directory to use for the build.
                                type: string
                            type: object
                          type: array
                        staticSite:
                          items:
                            properties:
                              buildCommand:
                                description: An optional build command to run while
                                  building this component from source. An optional
                                  build command to run while building this component
                                  from source.
                                type: string
                              catchallDocument:
                                description: The name of the document to use as the
                                  fallback for any requests to documents that are
                                  not found when serving this static site. The name
                                  of the document to use as the fallback for any requests
                                  to documents that are not found when serving this
                                  static site.
                                type: string
                              cors:
                                description: The CORS policies of the app.
                                items:
                                  properties:
                                    allowCredentials:
                                      description: Whether browsers should expose
                                        the response to the client-side JavaScript
                                        code when the request's credentials mode is
                                        include. This configures the Access-Control-Allow-Credentials
                                        header. Whether browsers should expose the
                                        response to the client-side JavaScript code
                                        when the request’s credentials mode is `include`.
                                        This configures the Access-Control-Allow-Credentials
                                        header.
                                      type: boolean
                                    allowHeaders:
                                      description: The set of allowed HTTP request
                                        headers. This configures the Access-Control-Allow-Headers
                                        header. The set of allowed HTTP request headers.
                                        This configures the Access-Control-Allow-Headers
                                        header.
                                      items:
                                        type: string
                                      type: array
                                    allowMethods:
                                      description: The set of allowed HTTP methods.
                                        This configures the Access-Control-Allow-Methods
                                        header. The set of allowed HTTP methods. This
                                        configures the Access-Control-Allow-Methods
                                        header.
                                      items:
                                        type: string
                                      type: array
                                    allowOrigins:
                                      description: The Access-Control-Allow-Origin
                                        can be The set of allowed CORS origins. This
                                        configures the Access-Control-Allow-Origin
                                        header.
                                      items:
                                        properties:
                                          exact:
                                            description: The Access-Control-Allow-Origin
                                              header will be set to the client's origin
                                              only if the client's origin exactly
                                              matches the value you provide. Exact
                                              string match.
                                            type: string
                                          prefix:
                                            description: Prefix-based match. Prefix-based
                                              match.
                                            type: string
                                          regex:
                                            description: The Access-Control-Allow-Origin
                                              header will be set to the client's origin
                                              if the client’s origin matches the regex
                                              you provide, in RE2 style syntax. RE2
                                              style regex-based match.
                                            type: string
                                        type: object
                                      type: array
                                    exposeHeaders:
                                      description: The set of HTTP response headers
                                        that browsers are allowed to access. This
                                        configures the Access-Control-Expose-Headers
                                        header. The set of HTTP response headers that
                                        browsers are allowed to access. This configures
                                        the Access-Control-Expose-Headers header.
                                      items:
                                        type: string
                                      type: array
                                    maxAge:
                                      description: 'An optional duration specifying
                                        how long browsers can cache the results of
                                        a preflight request. This configures the Access-Control-Max-Age
                                        header. Example: 5h30m. An optional duration
                                        specifying how long browsers can cache the
                                        results of a preflight request. This configures
                                        the Access-Control-Max-Age header. Example:
                                        `5h30m`.'
                                      type: string
                                  type: object
                                type: array
                              dockerfilePath:
                                description: The path to a Dockerfile relative to
                                  the root of the repo. If set, overrides usage of
                                  buildpacks. The path to a Dockerfile relative to
                                  the root of the repo. If set, overrides usage of
                                  buildpacks.
                                type: string
                              env:
                                description: Describes an app-wide environment variable
                                  made available to all components.
                                items:
                                  properties:
                                    key:
                                      description: The name of the environment variable.
                                        The name of the environment variable.
                                      type: string
                                    scope:
                                      description: The visibility scope of the environment
                                        variable. One of RUN_TIME, BUILD_TIME, or
                                        RUN_AND_BUILD_TIME (default). The visibility
                                        scope of the environment variable.
                                      type: string
                                    type:
                                      description: 'The domain type, which can be
                                        one of the following: The type of the environment
                                        variable.'
                                      type: string
                                  type: object
                                type: array
                              environmentSlug:
                                description: An environment slug describing the type
                                  of this app. An environment slug describing the
                                  type of this app.
                                type: string
                              errorDocument:
                                description: The name of the error document to use
                                  when serving this static site. The name of the error
                                  document to use when serving this static site.
                                type: string
                              git:
                                description: A Git repo to use as the component's
                                  source. The repository must be able to be cloned
                                  without authentication.  Only one of git, github
                                  or gitlab  may be set
                                items:
                                  properties:
                                    branch:
                                      description: The name of the branch to use.
                                        The name of the branch to use.
                                      type: string
                                    repoCloneUrl:
                                      description: The clone URL of the repo. The
                                        clone URL of the repo.
                                      type: string
                                  type: object
                                type: array
                              github:
                                description: A GitHub repo to use as the component's
                                  source. DigitalOcean App Platform must have access
                                  to the repository. Only one of git, github, gitlab,
                                  or image may be set.
                                items:
                                  properties:
                                    branch:
                                      description: The name of the branch to use.
                                        The name of the branch to use.
                                      type: string
                                    deployOnPush:
                                      description: Whether to automatically deploy
                                        new commits made to the repo. Whether to automatically
                                        deploy new commits made to the repo
                                      type: boolean
                                    repo:
                                      description: The name of the repo in the format
                                        owner/repo. The name of the repo in the format
                                        `owner/repo`.
                                      type: string
                                  type: object
                                type: array
                              gitlab:
                                description: A Gitlab repo to use as the component's
                                  source. DigitalOcean App Platform must have access
                                  to the repository. Only one of git, github, gitlab,
                                  or image may be set.
                                items:
                                  properties:
                                    branch:
                                      description: The name of the branch to use.
                                        The name of the branch to use.
                                      type: string
                                    deployOnPush:
                                      description: Whether to automatically deploy
                                        new commits made to the repo. Whether to automatically
                                        deploy new commits made to the repo
                                      type: boolean
                                    repo:
                                      description: The name of the repo in the format
                                        owner/repo. The name of the repo in the format
                                        `owner/repo`.
                                      type: string
                                  type: object
                                type: array
                              indexDocument:
                                description: The name of the index document to use
                                  when serving this static site. The name of the index
                                  document to use when serving this static site.
                                type: string
                              name:
                                description: The name of the app. Must be unique across
                                  all apps in the same account. The name of the component
                                type: string
                              outputDir:
                                description: 'An optional path to where the built
                                  assets will be located, relative to the build context.
                                  If not set, App Platform will automatically scan
                                  for these directory names: _static, dist, public.
                                  An optional path to where the built assets will
                                  be located, relative to the build context. If not
                                  set, App Platform will automatically scan for these
                                  directory names: `_static`, `dist`, `public`.'
                                type: string
                              routes:
                                description: (Deprecated - use ingress) An HTTP paths
                                  that should be routed to this component.
                                items:
                                  properties:
                                    path:
                                      description: The path to match on. Path specifies
                                        an route by HTTP path prefix. Paths must start
                                        with / and must be unique within the app.
                                      type: string
                                    preservePathPrefix:
                                      description: An optional boolean flag to preserve
                                        the path that is forwarded to the backend
                                        service. By default, the HTTP request path
                                        will be trimmed from the left when forwarded
                                        to the component. An optional flag to preserve
                                        the path that is forwarded to the backend
                                        service.
                                      type: boolean
                                  type: object
                                type: array
                              sourceDir:
                                description: An optional path to the working directory
                                  to use for the build. An optional path to the working
                                  directory to use for the build.
                                type: string
                            type: object
                          type: array
                        worker:
                          items:
                            properties:
                              alert:
                                description: Describes an alert policy for the app.
                                  Alert policies for the app component
                                items:
                                  properties:
                                    disabled:
                                      description: 'Determines whether or not the
                                        alert is disabled (default: false).'
                                      type: boolean
                                    operator:
                                      description: The operator to use. This is either
                                        of GREATER_THAN or LESS_THAN.
                                      type: string
                                    rule:
                                      description: 'The type of the alert to configure.
                                        Top-level app alert policies can be: DEPLOYMENT_FAILED,
                                        DEPLOYMENT_LIVE, DOMAIN_FAILED, or DOMAIN_LIVE.'
                                      type: string
                                    value:
                                      description: The value of the environment variable.
                                      type: number
                                    window:
                                      description: 'The time before alerts should
                                        be triggered. This is may be one of: FIVE_MINUTES,
                                        TEN_MINUTES, THIRTY_MINUTES, ONE_HOUR.'
                                      type: string
                                  type: object
                                type: array
                              buildCommand:
                                description: An optional build command to run while
                                  building this component from source. An optional
                                  build command to run while building this component
                                  from source.
                                type: string
                              dockerfilePath:
                                description: The path to a Dockerfile relative to
                                  the root of the repo. If set, overrides usage of
                                  buildpacks. The path to a Dockerfile relative to
                                  the root of the repo. If set, overrides usage of
                                  buildpacks.
                                type: string
                              env:
                                description: Describes an app-wide environment variable
                                  made available to all components.
                                items:
                                  properties:
                                    key:
                                      description: The name of the environment variable.
                                        The name of the environment variable.
                                      type: string
                                    scope:
                                      description: The visibility scope of the environment
                                        variable. One of RUN_TIME, BUILD_TIME, or
                                        RUN_AND_BUILD_TIME (default). The visibility
                                        scope of the environment variable.
                                      type: string
                                    type:
                                      description: 'The domain type, which can be
                                        one of the following: The type of the environment
                                        variable.'
                                      type: string
                                  type: object
                                type: array
                              environmentSlug:
                                description: An environment slug describing the type
                                  of this app. An environment slug describing the
                                  type of this app.
                                type: string
                              git:
                                description: A Git repo to use as the component's
                                  source. The repository must be able to be cloned
                                  without authentication.  Only one of git, github
                                  or gitlab  may be set
                                items:
                                  properties:
                                    branch:
                                      description: The name of the branch to use.
                                        The name of the branch to use.
                                      type: string
                                    repoCloneUrl:
                                      description: The clone URL of the repo. The
                                        clone URL of the repo.
                                      type: string
                                  type: object
                                type: array
                              github:
                                description: A GitHub repo to use as the component's
                                  source. DigitalOcean App Platform must have access
                                  to the repository. Only one of git, github, gitlab,
                                  or image may be set.
                                items:
                                  properties:
                                    branch:
                                      description: The name of the branch to use.
                                        The name of the branch to use.
                                      type: string
                                    deployOnPush:
                                      description: Whether to automatically deploy
                                        new commits made to the repo. Whether to automatically
                                        deploy new commits made to the repo
                                      type: boolean
                                    repo:
                                      description: The name of the repo in the format
                                        owner/repo. The name of the repo in the format
                                        `owner/repo`.
                                      type: string
                                  type: object
                                type: array
                              gitlab:
                                description: A Gitlab repo to use as the component's
                                  source. DigitalOcean App Platform must have access
                                  to the repository. Only one of git, github, gitlab,
                                  or image may be set.
                                items:
                                  properties:
                                    branch:
                                      description: The name of the branch to use.
                                        The name of the branch to use.
                                      type: string
                                    deployOnPush:
                                      description: Whether to automatically deploy
                                        new commits made to the repo. Whether to automatically
                                        deploy new commits made to the repo
                                      type: boolean
                                    repo:
                                      description: The name of the repo in the format
                                        owner/repo. The name of the repo in the format
                                        `owner/repo`.
                                      type: string
                                  type: object
                                type: array
                              image:
                                description: An image to use as the component's source.
                                  Only one of git, github, gitlab, or image may be
                                  set.
                                items:
                                  properties:
                                    deployOnPush:
                                      description: Whether to automatically deploy
                                        new commits made to the repo. Configures automatically
                                        deploying images pushed to DOCR.
                                      items:
                                        properties:
                                          enabled:
                                            description: Whether to automatically
                                              deploy images pushed to DOCR. Whether
                                              to automatically deploy images pushed
                                              to DOCR.
                                            type: boolean
                                        type: object
                                      type: array
                                    registry:
                                      description: The registry name. Must be left
                                        empty for the DOCR registry type. Required
                                        for the DOCKER_HUB registry type. The registry
                                        name. Must be left empty for the DOCR registry
                                        type.
                                      type: string
                                    registryType:
                                      description: The registry type. One of DOCR
                                        (DigitalOcean container registry) or DOCKER_HUB.
                                        The registry type.
                                      type: string
                                    repository:
                                      description: The repository name. The repository
                                        name.
                                      type: string
                                    tag:
                                      description: The repository tag. Defaults to
                                        latest if not provided. The repository tag.
                                        Defaults to latest if not provided.
                                      type: string
                                  type: object
                                type: array
                              instanceCount:
                                description: The amount of instances that this component
                                  should be scaled to. The amount of instances that
                                  this component should be scaled to.
                                type: number
                              instanceSizeSlug:
                                description: 'The instance size to use for this component.
                                  This determines the plan (basic or professional)
                                  and the available CPU and memory. The list of available
                                  instance sizes can be found with the API or using
                                  the doctl CLI (doctl apps tier instance-size list).
                                  Default: basic-xxs The instance size to use for
                                  this component.'
                                type: string
                              logDestination:
                                description: Describes a log forwarding destination.
                                  Logs
                                items:
                                  properties:
                                    datadog:
                                      description: Datadog configuration. Datadog
                                        configuration.
                                      items:
                                        properties:
                                          apiKey:
                                            description: Datadog API key. Datadog
                                              API key.
                                            type: string
                                          endpoint:
                                            description: Papertrail syslog endpoint.
                                              Datadog HTTP log intake endpoint.
                                            type: string
                                        type: object
                                      type: array
                                    logtail:
                                      description: Logtail configuration. Logtail
                                        configuration.
                                      items:
                                        properties:
                                          token:
                                            description: Logtail token. Logtail token.
                                            type: string
                                        type: object
                                      type: array
                                    name:
                                      description: The name of the app. Must be unique
                                        across all apps in the same account. Name
                                        of the log destination
                                      type: string
                                    papertrail:
                                      description: Papertrail configuration. Papertrail
                                        configuration.
                                      items:
                                        properties:
                                          endpoint:
                                            description: Papertrail syslog endpoint.
                                              Papertrail syslog endpoint.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              name:
                                description: The name of the app. Must be unique across
                                  all apps in the same account. The name of the component
                                type: string
                              runCommand:
                                description: An optional run command to override the
                                  component's default. An optional run command to
                                  override the component's default.
                                type: string
                              sourceDir:
                                description: An optional path to the working directory
                                  to use for the build. An optional path to the working
                                  directory to use for the build.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  updatedAt:
                    description: The date and time of when the app was last updated.
                      The date and time of when the App was last updated
                    type: string
                  urn:
                    description: The uniform resource identifier for the app. The
                      uniform resource identifier for the app
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time this condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: A Message containing details about this condition's
                        last transition from one status to another, if any.
                      type: string
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: Type of this condition. At most one of each condition
                        type may apply to a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
