---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: knowledgebases.gradientai.digitalocean.crossplane.io
spec:
  group: gradientai.digitalocean.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - do
    kind: KnowledgeBase
    listKind: KnowledgeBaseList
    plural: knowledgebases
    singular: knowledgebase
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: KnowledgeBase is the Schema for the KnowledgeBases API. <no value>
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KnowledgeBaseSpec defines the desired state of KnowledgeBase
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  addedToAgentAt:
                    description: The time when the knowledge base was added to the
                      agent.
                    type: string
                  databaseId:
                    description: The unique identifier of the DigitalOcean OpenSearch
                      database this knowledge base will use
                    type: string
                  datasources:
                    description: Data sources for the knowledge base
                    items:
                      properties:
                        fileUploadDataSource:
                          description: File upload data source configuration
                          items:
                            properties:
                              originalFileName:
                                description: The original name of the uploaded file
                                type: string
                              sizeInBytes:
                                description: The size of the file in bytes
                                type: string
                              storedObjectKey:
                                description: The stored object key for the file
                                type: string
                            type: object
                          type: array
                        lastIndexingJob:
                          description: Last indexing job for the data source
                          items:
                            properties:
                              completedDatasources:
                                description: Number of completed datasources in the
                                  last indexing job
                                type: number
                              dataSourceUuids:
                                description: Datasource UUIDs for the last indexing
                                  job
                                items:
                                  type: string
                                type: array
                              phase:
                                description: Phase of the last indexing job
                                type: string
                              tokens:
                                description: Number of tokens processed in the last
                                  indexing job
                                type: number
                              totalDatasources:
                                description: Total number of datasources in the last
                                  indexing job
                                type: number
                              uuid:
                                description: UUID  of the last indexing job
                                type: string
                            type: object
                          type: array
                        spacesDataSource:
                          description: Spaces data source configuration
                          items:
                            properties:
                              bucketName:
                                description: The name of the Spaces bucket
                                type: string
                              itemPath:
                                description: The path to the item in the bucket
                                type: string
                              region:
                                description: The region of the Spaces bucket
                                type: string
                            type: object
                          type: array
                        uuid:
                          description: UUID of the Knowledge Base
                          type: string
                        webCrawlerDataSource:
                          description: Web crawler data source configuration
                          items:
                            properties:
                              baseUrl:
                                description: The base URL to crawl
                                type: string
                              crawlingOption:
                                description: |-
                                  Options for specifying how URLs found on pages should be handled.
                                  - UNKNOWN: Default unknown value
                                  - SCOPED: Only include the base URL.
                                  - PATH: Crawl the base URL and linked pages within the URL path.
                                  - DOMAIN: Crawl the base URL and linked pages within the same domain.
                                  - SUBDOMAINS: Crawl the base URL and linked pages for any subdomain.
                                type: string
                              embedMedia:
                                description: Whether to embed media content
                                type: boolean
                            type: object
                          type: array
                      type: object
                    type: array
                  embeddingModelUuid:
                    description: The unique identifier of the embedding model
                    type: string
                  isPublic:
                    description: Indicates whether the knowledge base is public or
                      private.
                    type: boolean
                  lastIndexingJob:
                    description: The last indexing job for the knowledge base.
                    items:
                      properties:
                        completedDatasources:
                          description: Number of completed datasources in the last
                            indexing job
                          type: number
                        dataSourceUuids:
                          description: Datasource UUIDs for the last indexing job
                          items:
                            type: string
                          type: array
                        phase:
                          description: Phase of the last indexing job
                          type: string
                        tokens:
                          description: Number of tokens processed in the last indexing
                            job
                          type: number
                        totalDatasources:
                          description: Total number of datasources in the last indexing
                            job
                          type: number
                        uuid:
                          description: UUID  of the last indexing job
                          type: string
                      type: object
                    type: array
                  name:
                    description: The name of the knowledge base.
                    type: string
                  projectId:
                    description: The unique identifier of the project to which the
                      knowledge base belongs.
                    type: string
                  projectIdRef:
                    description: Reference to a Project in project to populate projectId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  projectIdSelector:
                    description: Selector for a Project in project to populate projectId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  region:
                    type: string
                  tags:
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  vpcUuid:
                    description: The unique identifier of the VPC to which the knowledge
                      base belongs.
                    type: string
                  vpcUuidRef:
                    description: Reference to a VPC in vpc to populate vpcUuid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  vpcUuidSelector:
                    description: Selector for a VPC in vpc to populate vpcUuid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  addedToAgentAt:
                    description: The time when the knowledge base was added to the
                      agent.
                    type: string
                  databaseId:
                    description: The unique identifier of the DigitalOcean OpenSearch
                      database this knowledge base will use
                    type: string
                  datasources:
                    description: Data sources for the knowledge base
                    items:
                      properties:
                        fileUploadDataSource:
                          description: File upload data source configuration
                          items:
                            properties:
                              originalFileName:
                                description: The original name of the uploaded file
                                type: string
                              sizeInBytes:
                                description: The size of the file in bytes
                                type: string
                              storedObjectKey:
                                description: The stored object key for the file
                                type: string
                            type: object
                          type: array
                        lastIndexingJob:
                          description: Last indexing job for the data source
                          items:
                            properties:
                              completedDatasources:
                                description: Number of completed datasources in the
                                  last indexing job
                                type: number
                              dataSourceUuids:
                                description: Datasource UUIDs for the last indexing
                                  job
                                items:
                                  type: string
                                type: array
                              phase:
                                description: Phase of the last indexing job
                                type: string
                              tokens:
                                description: Number of tokens processed in the last
                                  indexing job
                                type: number
                              totalDatasources:
                                description: Total number of datasources in the last
                                  indexing job
                                type: number
                              uuid:
                                description: UUID  of the last indexing job
                                type: string
                            type: object
                          type: array
                        spacesDataSource:
                          description: Spaces data source configuration
                          items:
                            properties:
                              bucketName:
                                description: The name of the Spaces bucket
                                type: string
                              itemPath:
                                description: The path to the item in the bucket
                                type: string
                              region:
                                description: The region of the Spaces bucket
                                type: string
                            type: object
                          type: array
                        uuid:
                          description: UUID of the Knowledge Base
                          type: string
                        webCrawlerDataSource:
                          description: Web crawler data source configuration
                          items:
                            properties:
                              baseUrl:
                                description: The base URL to crawl
                                type: string
                              crawlingOption:
                                description: |-
                                  Options for specifying how URLs found on pages should be handled.
                                  - UNKNOWN: Default unknown value
                                  - SCOPED: Only include the base URL.
                                  - PATH: Crawl the base URL and linked pages within the URL path.
                                  - DOMAIN: Crawl the base URL and linked pages within the same domain.
                                  - SUBDOMAINS: Crawl the base URL and linked pages for any subdomain.
                                type: string
                              embedMedia:
                                description: Whether to embed media content
                                type: boolean
                            type: object
                          type: array
                      type: object
                    type: array
                  embeddingModelUuid:
                    description: The unique identifier of the embedding model
                    type: string
                  isPublic:
                    description: Indicates whether the knowledge base is public or
                      private.
                    type: boolean
                  lastIndexingJob:
                    description: The last indexing job for the knowledge base.
                    items:
                      properties:
                        completedDatasources:
                          description: Number of completed datasources in the last
                            indexing job
                          type: number
                        dataSourceUuids:
                          description: Datasource UUIDs for the last indexing job
                          items:
                            type: string
                          type: array
                        phase:
                          description: Phase of the last indexing job
                          type: string
                        tokens:
                          description: Number of tokens processed in the last indexing
                            job
                          type: number
                        totalDatasources:
                          description: Total number of datasources in the last indexing
                            job
                          type: number
                        uuid:
                          description: UUID  of the last indexing job
                          type: string
                      type: object
                    type: array
                  name:
                    description: The name of the knowledge base.
                    type: string
                  projectId:
                    description: The unique identifier of the project to which the
                      knowledge base belongs.
                    type: string
                  projectIdRef:
                    description: Reference to a Project in project to populate projectId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  projectIdSelector:
                    description: Selector for a Project in project to populate projectId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  region:
                    type: string
                  tags:
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  vpcUuid:
                    description: The unique identifier of the VPC to which the knowledge
                      base belongs.
                    type: string
                  vpcUuidRef:
                    description: Reference to a VPC in vpc to populate vpcUuid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  vpcUuidSelector:
                    description: Selector for a VPC in vpc to populate vpcUuid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              publishConnectionDetailsTo:
                description: |-
                  PublishConnectionDetailsTo specifies the connection secret config which
                  contains a name, metadata and a reference to secret store config to
                  which any connection details for this managed resource should be written.
                  Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  configRef:
                    default:
                      name: default
                    description: |-
                      SecretStoreConfigRef specifies which secret store config should be used
                      for this ConnectionSecret.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  metadata:
                    description: Metadata is the metadata for connection secret.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          Annotations are the annotations to be added to connection secret.
                          - For Kubernetes secrets, this will be used as "metadata.annotations".
                          - It is up to Secret Store implementation for others store types.
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: |-
                          Labels are the labels/tags to be added to connection secret.
                          - For Kubernetes secrets, this will be used as "metadata.labels".
                          - It is up to Secret Store implementation for others store types.
                        type: object
                      type:
                        description: |-
                          Type is the SecretType for the connection secret.
                          - Only valid for Kubernetes Secret Stores.
                        type: string
                    type: object
                  name:
                    description: Name is the name of the connection secret.
                    type: string
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                  This field is planned to be replaced in a future release in favor of
                  PublishConnectionDetailsTo. Currently, both could be set independently
                  and connection details would be published to both without affecting
                  each other.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.datasources is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.datasources)
                || (has(self.initProvider) && has(self.initProvider.datasources))'
            - message: spec.forProvider.embeddingModelUuid is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.embeddingModelUuid)
                || (has(self.initProvider) && has(self.initProvider.embeddingModelUuid))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.region is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.region)
                || (has(self.initProvider) && has(self.initProvider.region))'
          status:
            description: KnowledgeBaseStatus defines the observed state of KnowledgeBase.
            properties:
              atProvider:
                properties:
                  addedToAgentAt:
                    description: The time when the knowledge base was added to the
                      agent.
                    type: string
                  createdAt:
                    description: The time when the knowledge base was created.
                    type: string
                  databaseId:
                    description: The unique identifier of the DigitalOcean OpenSearch
                      database this knowledge base will use
                    type: string
                  datasources:
                    description: Data sources for the knowledge base
                    items:
                      properties:
                        createdAt:
                          description: Created At timestamp for the Knowledge Base
                          type: string
                        fileUploadDataSource:
                          description: File upload data source configuration
                          items:
                            properties:
                              originalFileName:
                                description: The original name of the uploaded file
                                type: string
                              sizeInBytes:
                                description: The size of the file in bytes
                                type: string
                              storedObjectKey:
                                description: The stored object key for the file
                                type: string
                            type: object
                          type: array
                        lastIndexingJob:
                          description: Last indexing job for the data source
                          items:
                            properties:
                              completedDatasources:
                                description: Number of completed datasources in the
                                  last indexing job
                                type: number
                              createdAt:
                                description: Created At timestamp for the last indexing
                                  job
                                type: string
                              dataSourceUuids:
                                description: Datasource UUIDs for the last indexing
                                  job
                                items:
                                  type: string
                                type: array
                              finishedAt:
                                description: Timestamp when the last indexing job
                                  finished
                                type: string
                              knowledgeBaseUuid:
                                description: UUID  of the Knowledge Base for the last
                                  indexing job
                                type: string
                              phase:
                                description: Phase of the last indexing job
                                type: string
                              startedAt:
                                description: Timestamp when the last indexing job
                                  started
                                type: string
                              tokens:
                                description: Number of tokens processed in the last
                                  indexing job
                                type: number
                              totalDatasources:
                                description: Total number of datasources in the last
                                  indexing job
                                type: number
                              updatedAt:
                                description: Timestamp when the last indexing job
                                  updated
                                type: string
                              uuid:
                                description: UUID  of the last indexing job
                                type: string
                            type: object
                          type: array
                        spacesDataSource:
                          description: Spaces data source configuration
                          items:
                            properties:
                              bucketName:
                                description: The name of the Spaces bucket
                                type: string
                              itemPath:
                                description: The path to the item in the bucket
                                type: string
                              region:
                                description: The region of the Spaces bucket
                                type: string
                            type: object
                          type: array
                        updatedAt:
                          description: Timestamp when the Knowledge Base was updated
                          type: string
                        uuid:
                          description: UUID of the Knowledge Base
                          type: string
                        webCrawlerDataSource:
                          description: Web crawler data source configuration
                          items:
                            properties:
                              baseUrl:
                                description: The base URL to crawl
                                type: string
                              crawlingOption:
                                description: |-
                                  Options for specifying how URLs found on pages should be handled.
                                  - UNKNOWN: Default unknown value
                                  - SCOPED: Only include the base URL.
                                  - PATH: Crawl the base URL and linked pages within the URL path.
                                  - DOMAIN: Crawl the base URL and linked pages within the same domain.
                                  - SUBDOMAINS: Crawl the base URL and linked pages for any subdomain.
                                type: string
                              embedMedia:
                                description: Whether to embed media content
                                type: boolean
                            type: object
                          type: array
                      type: object
                    type: array
                  embeddingModelUuid:
                    description: The unique identifier of the embedding model
                    type: string
                  id:
                    type: string
                  isPublic:
                    description: Indicates whether the knowledge base is public or
                      private.
                    type: boolean
                  lastIndexingJob:
                    description: The last indexing job for the knowledge base.
                    items:
                      properties:
                        completedDatasources:
                          description: Number of completed datasources in the last
                            indexing job
                          type: number
                        createdAt:
                          description: Created At timestamp for the last indexing
                            job
                          type: string
                        dataSourceUuids:
                          description: Datasource UUIDs for the last indexing job
                          items:
                            type: string
                          type: array
                        finishedAt:
                          description: Timestamp when the last indexing job finished
                          type: string
                        knowledgeBaseUuid:
                          description: UUID  of the Knowledge Base for the last indexing
                            job
                          type: string
                        phase:
                          description: Phase of the last indexing job
                          type: string
                        startedAt:
                          description: Timestamp when the last indexing job started
                          type: string
                        tokens:
                          description: Number of tokens processed in the last indexing
                            job
                          type: number
                        totalDatasources:
                          description: Total number of datasources in the last indexing
                            job
                          type: number
                        updatedAt:
                          description: Timestamp when the last indexing job updated
                          type: string
                        uuid:
                          description: UUID  of the last indexing job
                          type: string
                      type: object
                    type: array
                  name:
                    description: The name of the knowledge base.
                    type: string
                  projectId:
                    description: The unique identifier of the project to which the
                      knowledge base belongs.
                    type: string
                  region:
                    type: string
                  tags:
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  vpcUuid:
                    description: The unique identifier of the VPC to which the knowledge
                      base belongs.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
